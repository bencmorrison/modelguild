# ModelGuild — Behavioral Contract (the TS/MCP reference spec)

This began (M0) as the **frozen behavioral contract of the bash layer**, captured so the
TypeScript/MCP rewrite could be verified against it. **The rewrite is complete and the bash layer
retired at M12 (2026-07-23).** These C-items are no longer a bash-parity target — they are the
**behavioral specification the TypeScript implementation holds**, now verified by the TS test suite
under `test/` (the reference).
Milestone history is in git history.

- **The TypeScript implementation and `test/*.test.ts` are the reference.** Where this document and the
  code disagree, the code wins; fix the document.
- **Reading the `Oracle:` citations below.** Each group cites the *retired bash sources* (`ask.sh`,
  `log.sh`, `panel-models.sh`) the item was originally derived from — kept as provenance, not as a live
  file to consult. The behavior each item names now lives in `src/` and is asserted by the TS suite:
  policy → `src/policy.ts` (`test/policy.test.ts`); config/panel → `src/config.ts`
  (`test/config.test.ts`); evidence layer → `src/log.ts` (`test/log.test.ts`); the tools →
  `src/{consult,panel,research,delegate}.ts` and their tests.
- Items: areas **A–H**, clauses numbered from **C1** upward in the order they were added, plus the layered-resolution additions **C2b** and **C9b** (issue #19, 2026-07-25), the run-id grammar **C33a** (issue #73, 2026-07-28) and the v1-permission-surface pin **C69a** (issue #93, 2026-07-29) — all numbered as suffixes so no existing C-number shifts. **The installed-payload state clause C72** (issue #94, 2026-07-29) sits at the end of area F, next to the command surface it is about; it was **renumbered from C71** (issue #113, 2026-07-29), which two same-day merges had assigned to two clauses — the issue-#107 clause merged first and keeps C71. **A clause id is unique, and the letter suffix is PART of it** (`C32` and `C32a` are different ids, not one id twice) — asserted by `modelguild/tests/check-contract-counts.sh`, because a number cited as an identifier that resolves to two clauses makes every citation of it ambiguous in both directions. **No upper bound is restated here, deliberately** (amended 2026-07-28, issue #60): this line used to close the range at `C64`, and it went stale the day the approval bridge shipped C65–C69 below it. The clause list itself is the authority on where the numbering ends. **The live-activity clauses** (issue #20, 2026-07-27) sit at the end of area D — adjacent to the evidence layer because both are records of a call, and explicitly separated from it because they are not the same record. **Retired items:** the witness path (C18–C21 `--watch`, and the
  witness parts of C29/C45) shipped in bash but the automated witness was retired at M12; those items
  are historical. The **`guild-watch` agent def** and the **`/guild:witness` command** retired with the
  witness (M12), so three counts stated across this file were stale and are **amended inline** (issue
  #36, 2026-07-23): C41's command count is **eight**, not nine (`witness` gone — matches `src/init.ts`
  `COMMAND_DOCS` and the F-oracle "(8 files)"); C47's def count is **three**, not four (`guild-watch`
  gone); and the `guild-watch`/`--watch` rows of C15/C48/C49/C57 are marked historical. **C46's forward-looking "mixed transport is a surfaced error" sentence was deleted
  (maintainer-ratified 2026-07-22 — both bash and TS snapshot per CALL, so mixed transport breaks no
  guarantee).** Exit-code items (area H) described `ask.sh`; the MCP tools express the same verdicts as
  structured `isError` results with the exit code carried as an analogue (see `test/consult.test.ts`).

---

## A. Model policy semantics
*Oracle: `modelguild/ask.sh` (`_has_rules`, `policy_tier`, policy enforcement block); `run-tests.sh` cases 11, 12, 20, 20b, 20c; `modelguild/models.policy`.*

- **C1** (amended 2026-07-25, issue #19 — resolution is now **LAYERED**) — Policy resolution walks an ordered chain of files, **most-specific first**, and the **first matching rule anywhere in the chain wins**: project `modelguild/models.policy.local` → project `modelguild/models.policy` → global `~/.claude/modelguild/models.policy.local` → global `~/.claude/modelguild/models.policy` → default-allow. A `models.policy.local` joins the chain **only if it has ≥1 complete rule** (the `_has_rules` gate is kept). `$GUILD_POLICY` is a **single-FILE override**: when set it is the entire chain, with nothing layered beneath it. The pre-#19 behaviour — one root wins and its single file is the whole policy, so a project `modelguild/` shadowed the global one entirely — is **superseded**: the global install is the **baseline** and the project adds a stricter deny or a looser allow on top of it. (`resolvePolicyLayers` / `policyTierAcross` in `src/policy.ts`; `policyTier` remains the single-root view.)
- **C2** (amended 2026-07-25, issue #19) — An empty or comment-only `.local` (a `deny` with no pattern is not a rule) does **not** shadow the committed policy: it is excluded from the chain entirely, so it neither displaces a committed rule nor fails the chain closed as a malformed layer. A **ruleful** `.local` no longer *replaces* the committed file either — it sits **above** it, so a personal rule wins for the ids it names while committed rules keep binding for the ids it does not.
- **C2b** (new 2026-07-25, issue #19) — Fail-closed is **chain-wide**: every existing layer is parsed even after a rule has matched (as C6 already requires within one file), so a malformed active line or an existing-but-unreadable file in **any** consulted layer resolves to **deny** with the loud reason naming that file. Stated cost: a broken *global* policy denies in every project, not only in the global one — the fail-closed direction, with the offending file and line named.
- **C3** — Tiers are `deny` / `ask` / `allow`. Patterns are shell globs matched against the full `provider/model` id. **First matching rule wins**, top to bottom.
- **C4** — Default is **allow**: no policy file, empty model id, or no matching rule all resolve to `allow` (policy only ever restricts).
- **C5** — Fail-closed: an existing-but-unreadable policy file resolves to **deny**.
- **C6** — Fail-closed: any malformed active line (unknown tier, missing pattern, trailing non-comment token) resolves to **deny** — even when `-m` was omitted, the whole file is still parsed. A final line lacking a trailing newline is still read.
- **C7** — A `deny` model exits **3** before opencode is invoked (no argv reaches opencode). An `ask` model without `GUILD_CONFIRMED=1` exits **4**; with it, proceeds. Enforced as a hard backstop independent of Claude's own check.

## B. Model & config resolution
*Oracle: `modelguild/ask.sh` (`conf_get`, model precedence); `modelguild/panel-models.sh`; `run-tests.sh` cases 2, 15, 21, 21b, 21c, 21d, 21e.*

- **C8** — Default-model precedence: `-m` flag > `$GUILD_MODEL` env > `modelguild.conf.local` `GUILD_MODEL` > opencode's own default (empty).
- **C9** (amended 2026-07-25, issue #19 — resolution is now **LAYERED**) — Config file resolution: `$GUILD_CONF` if set is a **single-FILE override** (the whole answer, nothing layered under it); otherwise **every** layer's git-ignored `modelguild.conf.local` contributes — the global `~/.claude/modelguild/` read first as the baseline, the project's `modelguild/` overlaid on top. A key set in the project **wins**; a key the project leaves unset **falls through** to global. The overlay is C10's last-assignment-wins applied to the concatenated layers, so there is one parser and no second merge rule (`resolveConfFiles` / `readLayeredConfContents` in `src/config.ts`). Parsed, **never sourced** (no code execution).
- **C9b** (new 2026-07-25, issue #19) — Guild-root resolution is layered: the read layers are `<cwd>/modelguild/` (when present) over `~/.claude/modelguild/` (when present), most-specific first. `$GUILD_ROOT` is a **single-root override** — exactly one layer, no global baseline beneath it — so an explicitly pinned root stays hermetic; `guild_status`/`doctor` report the roots on disk it leaves unlayered. The **primary** root (`layeredRoots()[0]`) is unchanged in meaning and is where **writes** go: the evidence log's `logs/` directory (`layeredRoots` / `resolveGuildRoot` in `src/config.ts`).
- **C10** — Config parsing: only `KEY=value` lines; comments/blanks ignored; leading whitespace on key stripped; last assignment wins; one layer of surrounding single/double quotes stripped; a trailing `  # comment` on the value is stripped.
- **C11** — `conf_get` semantics are **byte-identical** across `ask.sh`, `panel-models.sh`, and `doctor.sh` (regression-pinned).
- **C12** — A model id beginning with `-` (from env or config, bypassing `-m`'s `need_arg`) is refused with exit **2** — it must not be emitted as an opencode flag. A value-taking flag whose value is missing or starts with `-` is a usage error (exit 1).
- **C13** — Panel set resolution (`panel-models.sh`): explicit args > `$GUILD_MODELS` env > `modelguild.conf.local` `GUILD_MODELS`; comma- or space-separated; order preserved.
- **C14** — Panel de-duplicates (first-seen order kept, each dropped dup warned); warns on `<2` distinct models; warns if all models share one provider prefix ("diversity theater"); warns on a token that is not `provider/model`; exit **2** if no models at all. It does **not** consult the policy (that is per-call in `ask.sh`).

## C. Agent selection
*Oracle: `modelguild/ask.sh` (flag parsing, watch block, fallback blocks); `AGENTS.md` agent notes; `run-tests.sh` cases 1, 3, 3b–3h, 4, 14, 14b–14e.*

- **C15** (amended 2026-07-23, issue #36 — `--watch`/`guild-watch` retired at M12) — Default agent is `guild-read`. `--edit` → `guild-build`; `--research` → `guild-research`; `--watch` → `guild-watch` (**retired** with the witness at M12 — no watch agent ships; historical). `-a <agent>` overrides; an unrecognized agent runs as-is with a stderr note.
- **C16** (amended 2026-07-23, issue #34 — supersedes the bash-era fallback) — **A missing hardened def is a REFUSAL, never a fallback.** All four model-calling tools — `guild_consult`, `guild_panel`, `guild_research`, `guild_delegate` — refuse **unconditionally** when their hardened def (`guild-read` for consult/panel; `guild-research`; `guild-build`) is absent from **both** the project `.opencode/agent/` **and** the global `${XDG_CONFIG_HOME:-~/.config}/opencode/agent/` (`resolveAgentDefDirs` + `hardenedDefPresentIn` in `src/config.ts`, mirroring opencode's own `--agent` resolution order; fail-closed if absent in both). The refusal is a structured `agent-def-missing` result, **exit-analogue 5** (C57), **no model call, nothing logged** (gap parity). There is **no** silent downgrade to a weaker/unrestricted agent: the bash-era fallbacks (`guild-read`/`guild-research` → `plan`, `guild-build` → `build`, loud warning) are **removed** — a probe confirmed opencode also hard-errors on a message naming an absent agent, but the pre-flight refusal is the version-independent guarantee. The presence check governs **only** the tool's refusal decision, not opencode's own `--agent` resolution. `guild_panel` refuses the whole panel with one check (every member uses `guild-read`); `guild_consult` refuses even on a `sessionId` continuation (the def governs the agent regardless of session reuse). **This is STAGE ONE of two (issue #111, C73):** a filesystem presence check cannot observe whether opencode is APPLYING the file it found, and a def whose frontmatter opencode cannot parse passes this check while resolving with no default-deny floor at all. C73 adds the resolved-config stage behind it; this one is unchanged and stays in front.
- **C17** (amended 2026-07-23 — superseded by C16's unconditional refusal) — The bash-era `GUILD_REQUIRE_HARDENED=1` opt-in (which turned a fallback into a hard exit-5) **no longer exists** and is read by no code in `src/`: with refusal now unconditional there is nothing to opt into. Historical.
- **C18** — `--watch` has **NO fallback**: a missing `guild-watch.md` is exit **5** always (even without `GUILD_REQUIRE_HARDENED`), because any weaker agent could read the source and "audit" that.
- **C19** — `--watch` requires the watcher model be named explicitly; if unresolved (no `-m`, `$GUILD_WATCH_MODEL`, or `$GUILD_MODEL`) exit **8** — an empty id would let opencode pick its own default, possibly Claude, unchecked.
- **C20** — `--watch` refuses a Claude model (`anthropic/*`, `*claude*`, `*Claude*`) with exit **8** unless `GUILD_CONFIRMED=1` (then warns). Watcher model prefers `$GUILD_WATCH_MODEL` (env/config) unless `-m` was explicit.
- **C21** — `--watch` refuses to run when the effective `GUILD_LOG_DIR` resolves outside `<install>/logs/**` (exit 5) — the watch agent cannot read outside that scope, so an evidence-free audit is refused rather than produced.

### Read root, and the write root (issues #96/#107 — `src/worktree.ts`, `src/servepool.ts`, `test/worktree.test.ts`, `test/delegate.test.ts` section H)

- **C70** (added 2026-07-29, issue #96) — **A READ tool may be pointed at a git worktree of THIS repository, and at nothing else.** **The fence is `git worktree list`, enforced at ONE choke point** (`resolveWorktreeTarget`; C33a's discipline applied to a second caller-supplied path): the target is realpath-resolved and must be an exact member of the worktrees of the repository the server already resolved. **A target that does not validate is a REFUSAL that names the path** (`worktree-invalid`, exit-analogue **null** — there is no bash counterpart), never a silent fall back to the project root. It is refused **before any log write** (C24 gap parity) and routes no call anywhere. **The agent-def pre-check moves with the child** (C16's presence check, resolved against the serve root) — a worktree with no hardened def is `agent-def-missing` naming *that worktree's* dir, not a turn that dies on an unresolvable agent. **Scope, deliberate:** `guild_consult`, `guild_panel` (panel-WIDE — one root for every member, since members reading different trees would be incomparable while appearing to disagree) and `guild_research`. **`guild_delegate` did NOT take it, and now does — see C71** (issue #107). **Mechanism:** one supervised child per root (`ServePool`), never a restart of the shared one — a restart would kill a serve with other calls in flight — and the pool dies with the primary child through a single `OpencodeLifecycle.onShutdown` companion rather than a second copy of the stdin-EOF/transport-close wiring. **That companion runs on TEARDOWN reasons only, and the exemption is part of this clause:** the exempt set is exactly `idle` and `per-call`, with an **absent or unrecognized reason counting as teardown**, so a later reason can cost a respawn but never an orphan; each extra reclaims itself on its own idle timer. **A CONTINUATION'S ROOT IS THE SESSION'S, NOT THE CALL'S.** `GET /session/{id}`'s `directory` is the authority: the continuation is routed there, an explicit `worktree` that disagrees is an ERROR naming both, and a session whose directory is not a worktree of this repository is a refusal, never a guess. An **unfetchable** session refuses only where the answer could differ — this call named a root, or another root has already been routed in this server's life; with neither true the primary is the only child, the lookup decides nothing, and the call proceeds as before #96, which is what keeps the worktree-free continuation flows (`/guild:collaborate`, `/guild:workshop`) from depending on a new endpoint. Cost, stated: every continuation makes one short control-plane GET **before** the policy gate, so a continuation naming a denied model ensures a serve is up on the way to being refused (no model turn, nothing logged); a fresh call is unchanged. **THE READ ROOT IS IN THE RECEIPTS.** The `started` entry carries `read_root` when a non-default root was used. It follows C29's optional-field rule: written only when supplied, so an untargeted call's entry is byte-identical to one written before the field existed and no `verify()` branch changes. **Honest bound, part of the contract:** this is a WIDENING of what the read paths can read, and therefore of what can egress to a third-party model provider (C49/C50) — everything those clauses say about credentials and the web now applies to the targeted worktree too. It is enumerable, it belongs to the repository the user is already in, and it is opt-in per call (no target ⇒ behaviour byte-identical to before), but it is not a confidentiality boundary and is not claimed as one. A targeted worktree that already contains `.opencode/` also gets opencode's `node_modules`/`package.json` scaffolding written into it, while one with no `.opencode/` is untouched — **git-invisible only where opencode wrote the ignore file itself**, so a repo that COMMITS its own `.opencode/.gitignore` sees three untracked entries appear and a read-only review therefore visibly mutates the tree under review.
- **C71** (added 2026-07-29, issue #107) — **`guild_delegate` MAY BE POINTED AT A WORKTREE TOO, AND THE CAPTURE MOVES WITH THE CHILD.** The same `worktree` input, the same single choke point (`resolveWorktreeTarget` — no second validation path, no second refusal shape), the same `worktree-invalid` refusal with exit-analogue **null**, refused before any log write **and before any snapshot**. **THE WHOLE CLAUSE IS THE COUPLING.** The root is resolved **once**, and the ONE value is threaded to the serve child *and* to every capture consumer: the pre-turn snapshot, the throwaway-index baseline and after-tree, the ignored-file fingerprint, the submodule state, `scaffoldDigest`, the recorded patch and the recovery hint. `deps.repoDir` becomes an **input** to that resolution rather than a fallback beneath it, and `resolveRepoDir` is consumed inline so it is never bound to a local (issue #107). **That is a discipline, not a proof, and the contract says so:** `cwd` legitimately remains in scope — the evidence log takes it, since the log root is deliberately NOT re-rooted at the target — so nothing in the type system prevents a future edit handing it to a capture consumer. **THE RECOVERY HINT NAMES THE TREE.** A targeted call's hint is `git -C <worktree> checkout <tree> -- <path>`; the bare C39 form would restore into whatever tree the reader is standing in, since a worktree shares its main checkout's object database — silently wrong. An untargeted call's hint is unchanged. **THE WRITE ROOT IS IN THE RECEIPTS, IN ITS OWN FIELD.** The `started` entry carries `write_root`, NOT a reuse of #96's `read_root`: "this tree was mutated" and "the answer describes this tree" are different claims. Same C29 optional-field rule — written only when a target was resolved, so an untargeted delegation's entries are byte-identical to pre-#107 and no `verify()` branch changes; the `delegate-diff` is paired to it by `call_id`. **NO SESSION-AUTHORITY LOOKUP, deliberately**: `guild_delegate` has no continuation (no `sessionId` input, a fresh session per call, not kept), so the lookup would decide nothing. **The approval bridge parses the def IN FORCE IN THE TARGET TREE** — `approvalFor` receives the re-rooted `agentDefDirs`, so review finding H3's invariant (never gate a tool the agent's own def denies) holds along this new axis. **THE PRECONDITION IS RECORDED AT BASELINE, NEVER INFERRED**: `WorktreeSnapshot.opencodeDir` captures whether `.opencode/` existed *before* the turn, because afterwards it exists either way. **The `scaffoldChanged` FLAG never varies** — it always fires — and only the WARNING TEXT varies, across **three** cases: an existing plugin dir *modified* (review it); scaffolding *appearing* where `.opencode/` was already present (opencode's own first-run materialization is a **sufficient** explanation, explicitly **not** a claim that it is the whole of what appeared); and scaffolding *created* where `.opencode/` was **absent** (opencode does not scaffold such a tree, so this is an **unexplained** write into a directory `opencode serve` loads and executes — no benign reading is offered). Pre-warming the child before the baseline snapshot was considered and rejected (issue #107). **Cost, stated and not softened:** a delegated model can now edit files and run `bash` in a worktree the caller named, so C49/C50's accepted trusted-repo exposure — `guild-build` reads any repo file including credentials (#29), and `bash` reaches the network regardless of the tool-layer denies — extends to that tree. **TWO ACCEPTED RESIDUALS, named rather than left implicit.** (1) **`GUILD_AGENT_DIR` short-circuits the re-rooting**: `resolveAgentDefDir` returns that override before `projectDir` is consulted, so with it set both the def presence pre-check *and* `resolveAgentAllowSet` read the override while opencode itself still resolves from the serve cwd + global dir — meaning C66's H3 invariant is then checked against a def that is not the one in force. (2) **The `delegate-diff` entry carries no root**: `write_root` rides on `started` only — but the entry a reader consults when asking *what did this patch change?* does not itself name the tree, and the patch file lives under the **project's** logs regardless of which tree was edited. The pairing is by `call_id`. **The human diff review remains the boundary — of a diff that must be of the tree actually edited**, which is why the coupling above is the contract and not an implementation detail.

### The resolved-agent floor (issue #111 — `src/agentfloor.ts`, `test/agentfloor.test.ts`)

- **C73** (added 2026-07-29, issue #111; numbered as a suffix to the area) — **THE DEF CHECK IS TWO STAGES, AND THE SECOND ONE ASKS OPENCODE.** C16's presence check answers *does the def file exist?*; it cannot answer *is opencode applying it?*, and the gap between those is total. Frontmatter opencode cannot parse is applied in **NO part**: the permission map is absent entirely, the agent resolves on opencode's built-in `"*": allow` with no default-deny floor, and it is **silent** — no warning, exit 0, and the file on disk still reads as hardened. A control `guild-read` resolved to 19 rules ending `"*": deny` + its allow-set; the same def with one duplicate key (top level **or** nested inside the permission map) or one tab-indented line resolved to the 13 built-ins alone. Those two shapes are the instances currently known and are **explicitly not a closed list** — the condition is the parse.
  **THE CHECK IS OF THE RESOLVED CONFIG, NOT OF THE SOURCE** (maintainer decision, Ben, 2026-07-29). **AND IT IS AN EFFECTIVE ACTION, NEVER A PRESENCE TEST.** The action is computed **last match wins**, over the rules that can answer for an arbitrary invocation, for a probe tool no hardened def grants, and must be `deny`; that also catches a later `"*": allow` re-opening everything. `modelguild/tests/check-agent-permissions.sh`'s `effective()` is the reference for the semantics and the probe literal is deliberately the same one. Two refinements are part of the clause: the probe is a **sentinel name that is not a tool at all**, because a real denied tool (`task`) would falsely refuse a user who legitimately re-scoped their own def to allow it; and a `"*"` rule with a **narrow pattern** is not counted as a floor. **The sentinel has its own false-refusal class**: a def written as a **denylist** — every unwanted tool denied by name, no `"*"` rule at all — resolves nothing for the probe, so the effective action is *undefined* and the call is refused as "nothing matched" even though opencode applies that def perfectly well.
  **SCOPE AND PLACEMENT.** Every tool that refuses on a missing def carries it: `guild_consult`, `guild_panel` (**once up front for the whole panel**), `guild_research`, `guild_delegate`. **The C16 filesystem presence check stays in front, unchanged** — this is additional, not a replacement. It runs **after** the model-policy gate (so a denied model is still refused without contacting opencode), and **before** the approval pre-flight, before `log.newRun()`, and — on the write path — **before the GIT worktree snapshot**: C24 gap parity, a refusal routes nothing and writes no evidence run. **THE SCAFFOLD BASELINE IS HOISTED ABOVE THE GATE, AND THAT ORDERING IS PART OF THIS CLAUSE** (added 2026-07-30, maintainer decision, review A1). `snapshotScaffold` (the `scaffoldDigest` plus whether `.opencode/` existed) is captured **before** the gate, `snapshotWorktree` takes it as a **required** parameter so an omission is a type error, and the git half stays after the gate. Ordering it ahead of the approval pre-flight is load-bearing rather than tidy: C66's never-widen intersection is computed from the def **source**, so on a voided def the bridge would compute the narrow written allow-set while opencode allowed everything.
  **THREE OUTCOMES.** (1) opencode answers and the floor is in force ⇒ proceed, with the result **byte-identical** to a pre-#111 one (no field is added). (2) opencode answers and the floor is **not** in force — including an agent it does not list at all, which would answer HTTP 500 mid-turn — ⇒ **REFUSE**, kind `agent-unhardened`, exit-analogue **null** (deliberately **not** a reuse of C57's 5, which means specifically *the def is missing*), with a message naming the agent, the action actually resolved, the general catch-all rules in force, and the likely cause. (3) **opencode cannot be asked at all** — the control-plane call fails, or answers a shape the check cannot read ⇒ **PROCEED, AND SAY SO**, never silently: `agentUnverified` on the tool result (`structuredContent.agentUnverified`) **and** one stderr line stating the direction taken. **An `unverified` run with the APPROVAL BRIDGE ARMED loses more than that, so the note says so**: `agentUnverified` must state the gating may be **narrower than the set of tools opencode will actually run**. **The parse is strict in the same direction, everywhere:** one rule entry the check cannot read fails the **whole** array as unreadable rather than being dropped, because the entry dropped could be the floor — and a rule carrying **no `pattern`** is unreadable too, rather than being read as the universal one.
  **IT IS VERIFIED TWICE: ONCE BEFORE THE GATES, ONCE INSIDE THE TURN'S OWN SERVE LEASE** (added 2026-07-30, maintainer decision, review A3). `askViaAgent` re-verifies from inside the lease that will carry the session and the message (`PreTurnAgentCheck`), against the handle it is about to use, before the session is created. On the shared long-lived child that is a **cache hit and costs nothing**; under per-call mode it is a real check on the child that serves. Only an `unhardened` verdict stops the turn there — an `unverified` one must not — but it must still reach the caller: announcing is decided by the entry point against an `announced` set the CALL owns — deduped on the **child instance** within one call, not on the call — and the late verdict propagates into `structuredContent.agentUnverified`. **The late refusal also carries its own message unwrapped**, like the early shape, rather than the generic "the call failed" envelope. **THE CARDINALITY IS PART OF THE CLAUSE.** A late refusal lands after `log.expect()` and `log.started()`, and C24 requires exactly one `expected-call`, one `started` and one `completed` per `call_id`, checked in BOTH directions — so a refusal that merely returned would leave a run that FAILS `verify()`. It therefore throws (`AgentFloorNotInForceError`) and flows through the lifecycle's existing catch, which writes `completed` (exit 1, `capture_state: failed`) exactly as a model failure does: three entries, one `call_id`, and `verify()` returns the ORDINARY failed-capture verdict rather than a missing-entry one. On a panel it is a **per-member** kind, not a panel-wide refusal, because by then each member holds its own lease. The EARLY gate is retained precisely because it keeps the common refusal free of any evidence-log footprint at all.
  **WHAT IS CACHED IS `verified` AND NOTHING ELSE, KEYED ON THE CHILD INSTANCE.** A non-`verified` verdict is dropped and re-computed on the next call, and the warning **repeats per call** — that is the correct behaviour for "never silently", not a regression. The key is the child INSTANCE, not its URL: `ServeHandle.instanceId` is a per-process monotonic counter minted per spawn, so two handles carry it iff they are the same child, and a **base URL is a reusable loopback port, not an identity**. The URL remains a second key component, which can only make the key stricter (a miss, never a stale hit). **RETAINING `verified` PER CHILD IS CORRECT BY CONSTRUCTION:** the check and the enforcement read the SAME spawn-time config, and there is no state in which this reports `deny` while the child enforces `allow`. **The cost lands on RECOVERY instead, and it is real:** a user who does what the refusal says — fix the def, re-run `init` — **stays refused**, because the stale child is still serving, and `withServe` re-arms the idle timer on every entry, so retrying keeps that child alive past `GUILD_SERVE_IDLE_MS`. The refusal's remedy therefore **names getting a fresh child** and says why. Killing the child on an `unhardened` verdict was considered and deliberately **not** done: that is C70's finding-H1 shape (tearing down a child that may have calls in flight) and is a separate decision.
  **Honest bound:** the check is authoritative about what opencode **resolved** and says nothing about opencode's **enforcement** of it — `verify-guild-*.sh` remains the runtime proof — and C71's `GUILD_AGENT_DIR` residual is untouched, since this stage never reads a def file at all. Under `GUILD_SERVE_PER_CALL=1` the child that is CHECKED by the early gate is not the child that runs the turn; the verdict still transfers in practice, but nothing here may claim the checked child is the serving child.

## D. Evidence layer
*Oracle: `modelguild/log.sh`; `modelguild/ask.sh` (logging hooks); `run-tests.sh` log block (~L994–1620); `AGENTS.md`.*

- **C22** — Every model call writes **exactly three** lifecycle entries sharing one `call_id`: `expected-call` (before capture setup) → `started` (before the call) → `completed` (after). Three calls ⇒ nine entries.
- **C23** — Entries are paired by `call_id`, never by run_id+turn (retries and concurrent panel calls break turn-based pairing). `turn` is recorded for readability only.
- **C24** — `verify` requires, per `call_id`, **exactly one** of each of expected/started/completed — checked in **both directions** (a lone `completed` fails; duplicates fail; set-membership is insufficient).
- **C25** — `raw_response` is recorded **in full, always**, byte-exact via `jq --rawfile` (a `$(...)` capture that strips trailing newlines is a contract violation). `completed` carries `capture_state` ∈ {`complete`,`failed`}; a `complete` with a missing/absent response file downgrades to `failed`.
- **C26** — Prompt privacy `GUILD_LOG_PROMPTS` ∈ {`full` (default), `hash`, `off`}: `full` stores prompt text + digest; `hash` stores only a 64-hex digest; `off` stores neither. `verify` enforces each mode's shape.
- **C27** — Integrity is a hash chain (`prev_hash` = sha256 of prior line) **plus** a per-entry self-hash (`entry_hash` over the payload) covering the tail line the chain cannot. Hashes catch accidental corruption only — **not** adversarial rewriting.
- **C28** — `verify` also checks: valid JSONL, unique non-empty `expected-call` call_ids, capture completeness, prompt-mode/hash consistency, and referenced `delegate-diff` patch present + hash-matching. Integrity failure exits **7**.
- **C29** (amended 2026-07-28, issue #74 — the patch-less `delegate-diff`) — Entry types and claim flags: `claude-final` (the summary the developer read); `claude-disposition` **`claim:true`** (Adopt/Adapt/Reject/Defer, self-report to audit); `subagent-voice` **`claim:true, captured:false`** (a Claude subagent's turn, text in `claimed_response` not `raw_response`); `delegate-diff` **`claim:false`** (machine evidence, patch hash verified). Verdicts are validated to the four-value set. **A `delegate-diff` may carry NO patch artifact** (`EvidenceLog.diffUncaptured`): when the capture machinery itself throws there is nothing to hash, so the entry records `patch: null`, `patch_bytes: 0`, `files_changed: 0`, a **required** non-empty `incomplete_reason`, and **no `response_hash`**. That last field is what keeps verify's two branches apart: `capture_complete:false` fails the run on the **capture-completeness** check (the intended loud failure), while the referenced-artifact check — gated on a non-empty `response_hash` — never runs, so the entry is never reported as pointing at a MISSING patch it never claimed to have. Optional `incomplete_detail` (bounded crash text) and `scaffold_changed` are written only when supplied, so an entry without them stays shape-identical to one written before they existed.
- **C30** — A run of only well-formed `subagent-voice` entries verifies as non-empty (all-Anthropic exchange). Completeness bound: an unlogged subagent voice is undetectable (no durable pre-spawn marker), unlike a crashed opencode call which leaves its `expected-call`.
- **C31** (amended 2026-07-28, issue #74 — the "lost entry" guarantee is SCOPED) — Logging is best-effort and **must never fail the call it records**: every hook (including `mktemp`s) is guarded; the response `tee`'s status is read from `PIPESTATUS[0]`. A lost entry surfaces as a `verify` gap, never as a non-zero call exit or a silent success — **but that holds only for entries a cardinality rule requires.** C24 requires exactly one `expected-call`/`started`/`completed` per `call_id`, so losing any of those three is a gap `verify` reports. **Nothing requires a `delegate-diff`**, so a lost one is a *silent clean*: the run verifies, and the log cannot tell you an entry was ever owed. That was true before this amendment and simply unstated. The write path's compensation is `capture.recordFailed` on the tool result (C40) — not a `verify` gap, and deliberately not dressed up as one.
- **C32** (amended 2026-07-25, issue #23 — enforcement points, age rule, scope fences) — Retention: run dirs older than `GUILD_LOG_RETENTION_DAYS` (default 14; 0 disables) are pruned at **three** points, all resolving the window through one resolver so they cannot disagree: `new-run` (as before), **MCP server start** (added — a session that started the server and made no model call pruned nothing, so a project you stopped consulting kept its logs forever), and the **`modelguild logs clean`** CLI. Server start is **skipped** when `GUILD_LOG=off`: a user who turned logging off to freeze what is on disk keeps it. Prune only touches directories matching a minted run-id shape, only as **direct children** of the one resolved logs root (`GUILD_LOG_DIR` + `GUILD_LOG_PARTITION` applied — under partitioning only the current project's partition is in scope), and only where `lstat` says a real directory (a run-id-shaped **symlink** is never followed nor removed). A run's **age is the newest mtime found by a BOUNDED walk of its contents**, not the run dir's own mtime — a long-lived run whose `calls.jsonl` is still being appended is never pruned out from under itself. The walk's bounds are part of the contract, because they are what a maintenance path may not hang on: it descends at most **eight** levels below the run dir, `lstat`s throughout so a **directory symlink is counted but never descended** (it cannot leave the run dir or loop), and **skips an entry it cannot read**. Cost, stated honestly: a file that is newer than everything else in the run but sits deeper than that bound, or behind a symlinked directory, does not hold the run open — the age reads older than it is and the run can be pruned. The shipped layout is ONE level deep (`calls.jsonl`, `activity.jsonl`, `approvals.jsonl`, `meta.json`, `diff-*.patch`, and a `reports/` subdirectory), so the bound is a fence against a surprise tree, not a normal-case behaviour. A window that is **not a whole number** (a typo) resolves to *disabled*, not to the default: what cannot be read is never a licence to delete. Prune returns a result (removed run ids, ages, bytes, kept/skipped counts) and **never throws** — C31's posture extended to the maintenance path — and a `latest` symlink left dangling by a prune is dropped. **Layering (C9b) splits read from write here as it does everywhere:** the window is READ across every root layer (a global `GUILD_LOG_RETENTION_DAYS` binds in a project that never restates it), but only the **primary** root's `logs/` is pruned — that is the only root runs are ever written to, so the other layers hold no runs of ours and this project's prune has no business deleting from them.
- **C32a** (issue #23) — `modelguild logs clean [--days N] [--dry-run] [--dir D]`: with `--days` it uses that window; without it, the configured one. It **refuses** (exit 2, with the reason) when the configured window is `0`/disabled or unparseable, and when `--days 0` is passed explicitly — "no window" is never read as "delete everything". `--dry-run` reports what would be removed and deletes nothing. Exit 0 on success (including a logs dir that does not exist yet); 2 on a usage error or a refusal.
- **C33** — Partitioning: `GUILD_LOG_PARTITION=1` narrows the root to `<base>/<project-key>/<run>` (key = git top-level else CWD, base + short path-hash); stays under `modelguild/logs/`; disabled when `GUILD_LOG_DIR` is set explicitly. Default OFF ⇒ byte-identical to today.
- **C33a** (added 2026-07-28, issue #73 — run-id validation) — **A run id is a DIRECTORY NAME under the resolved logs root, so its shape is part of the contract.** C33 fences the root; this fences the child component. It is validated at **one choke point** (`#resolveRun` in `src/log.ts`) covering **both** supplied paths — the caller's `runId` argument and `$GUILD_RUN_ID`. The grammar is a single conservative path segment — an alphanumeric first character, then letters/digits/`.`/`_`/`-`, at most **128** characters, no `..` anywhere in it, and not a **reserved name** — `latest` (the symlink C34 maintains) or `watchers` (the approval bridge's presence dir, C67), compared **case-insensitively** — which excludes `/`, `\`, NUL and every other control character, a leading `.`, and whitespace. The minted shape (`<UTC stamp>-<hex>`) passes by construction. **Anything else is an ERROR, never a silent fresh-run fallback.** An EMPTY `runId` keeps its existing meaning of "absent, mint a fresh run". Four surfaces, each keeping the posture area D already holds elsewhere: an MCP tool's `runId` **input** is refused as a tool input error naming the tool, the field and the rule (`resolveRunIdArg`) **before** any work is done; the evidence layer's own refusal keeps **C31** (`ok:false` + a stderr warning naming the source — `runId` vs `GUILD_RUN_ID` — never an exception into the call) and writes **nothing at all**, not even a fallback run; `verify()` returns **code 7** rather than throwing (C31's audit-path posture, as in the IO case); and `modelguild watch --run` — the one place a human types a run id by hand — is a **usage error, exit 2**. **Bound, stated:** with `GUILD_LOG=off` nothing is written, so an invalid run id is simply never used; the tool-input refusal fires regardless of the logging state. `guild_delegate` applies the guard (`log.enabled() && runId.length > 0`) before touching the run dir, and reports the absent capture as `captureComplete:false` / `incompleteReason:"logging-off"` with the recovery hint — the model's work is never discarded by a logging-path failure (C31). **Same shape, two other components:** the `latest` symlink's target is read from disk and joined by `modelguild watch` without passing through `dir()`, so `latest()` returns a name only when it satisfies this grammar (an escaping target reads as *no latest run*); and `verify()` **basenames** a `delegate-diff` entry's `patch` field on READ as `diff()` already does on write, so a doctored entry cannot point the audit at a file outside the run dir on its way to failing the hash check. **Retention gap, stated:** C32's prune fence is a *different, narrower* predicate — the minted shape only — so a run id that satisfies this grammar without being minted (`my.audit.2026`) is **never pruned**, and its `calls.jsonl` can hold full prompts under `GUILD_LOG_PROMPTS=full`. The fence is not widened to this grammar (issue #73): sparing the user's own directories is why it exists. **That gap is a decided question, not an open one (maintainer, 2026-08-02, issue #134): the grammar stays as it is.** A hand-written label such as `--run my-audit` or `GUILD_RUN_ID=release-check` remains unpruned by design, accepted and priced rather than left as an oversight, and the cost is that such a run's `calls.jsonl` lives until deleted by hand. Tightening the grammar to the minted shape would close the gap by removing that capability, which is why it was declined.
- **C34** (amended 2026-07-28, issue #80 — multi-session on one root) — Concurrency: appends are serialized by a portable `mkdir` lock (stale-lock steal after ~1 min; drop-the-entry after ~10s rather than a torn unlocked append). That lock is **per run file**, which is precisely why it does not cover the multi-session case: two Claude Code sessions open on one project run two servers, and each writes its **own** run into **one** logs root — `calls.jsonl` is never contended between them, and the shared state is the root itself: the `latest` symlink and the retention scan. All three are part of the contract. (1) `latest` names the most recent run, and its refresh is **atomic** — a symlink at a temp name renamed over the old one — and **skipped when the link already names this run**. `ln -sfn`'s unlink-then-symlink pair leaves a window in which `latest` does not exist at all, and the refresh runs on **every** append, so a reader that re-resolves `latest` on every poll (`modelguild watch`, C63) genuinely lands in it — a hammering reader caught it absent repeatedly against racing sessions before this. A filesystem that cannot rename over a symlink falls back to the pair. **The atomicity is `rename(2)`'s and therefore a PLATFORM property**: POSIX specifies the replacement as atomic, and this repo exercises it on Linux only — the node suites do not run in the macOS CI job — so it is relied on, not proven here on both platforms. (2) **`latest()` answers only when the run directory is still there.** Prune deletes run dirs and only afterwards drops a dangling `latest`, and a second session may be pruning while this one reads — so the pointer names a deleted run for a real window, and answering with it would send a watcher to tail a file that is gone. A stale pointer therefore reads as *no latest run*, never as a run that vanished. The dangling drop re-reads the link immediately before unlinking, so it will not delete a pointer another session just repointed at a live run of its own: a **narrowing, not an elimination** — there is no unlink-if-target-matches syscall, which is why the existence check above is where the guarantee actually lives and the drop is **hygiene** (it keeps a human listing the root from finding a pointer to nothing). **There is no backstop for the remainder, and an earlier draft of this clause wrongly claimed one** ("the next append repoints the link anyway"): that is false exactly when the racing append was the run's LAST one — the `completed` of the final call, the ordinary way a run ends — after which `latest` stays absent until a later run starts and `modelguild watch` reports "no runs yet" for a run that finished fine. Sub-microsecond window, no evidence at risk, stated rather than papered over. (3) Retention passes racing each other and racing writers stay **data, never an exception** (C32's posture): a run being written is kept by the newest-content age rule, and a pass whose removal fails records the run as `skipped` rather than throwing. Two honest limits on the accounting, both observed rather than assumed: a run **can be counted removed by more than one pass** — `rmSync` with `force` swallows ENOENT, so two passes that both stat'd it before either deleted it both report it — so `removed` counts *removals attempted and not refused*, not distinct runs; and a concurrent removal (or a lock/run-dir `mkdir` landing inside the tree mid-walk) can surface ENOTEMPTY/EBUSY, which is why the removal **retries** and, if it still fails, leaves a partly-emptied dir that the **next** pass finishes — the run is still old and still in scope. Held by `test/log.test.ts` with real racing **processes** — synchronous fs calls inside one process can never interleave, so a same-process test would prove nothing here — with a reader hammering `latest` throughout.
- **C35** — Config knobs resolve **env override > `modelguild.conf.local` > default**, identically to `ask.sh`. Subcommands: `new-run`, `latest`, `dir`, `path`, `expect`, `started`, `completed`, `final`, `disposition`, `subagent-voice`, `diff`, `verify`, `prune` (usage error exits 2).

### The empty answer (issues #117, #121 — `src/client.ts`, `src/consult.ts`, `src/delegate.ts`)

- **C74** (added 2026-07-30, issue #117; **write path added 2026-08-02 and REDESIGNED 2026-08-03 after an adversarial review round, issue #121**) — **A TURN THAT PRODUCES NO ANSWER IS A FAILURE, NOT AN EMPTY SUCCESS — and the two paths ask a different question because their answer is a different thing.**
  - **READ PATHS (unchanged).** Both history extractors — the final text and the serving agent — stop at the last `user` message, so a continuation can never answer, or validate its agent, from an earlier turn. `guild_consult`/`guild_panel`/`guild_research` opt in (`requireAnswer`): an empty or whitespace-only final text is refused as `empty-answer`, exit-analogue null, quoting the turn's own `info.error` when opencode carried one. Such a panel member carries `error`, no `text`, no `sessionId`; a continued session is KEPT even under `keepSession:false`. `completed` keeps `capture_state:"complete"` and the byte-exact blank `raw_response` with `exit_code:1`, which `verify()` never reads, so the run verifies clean. **Bound: the unit is the TURN** — a preamble before a dead final message passes, returning the preamble. **THE REFUSAL NOW DIAGNOSES RATHER THAN ONLY REFUSING (issue #168, 2026-08-15), on BOTH paths.** `empty-answer` and `empty-delegation` carry `error.diagnostics`, structurally and in the message text, per panel MEMBER: the turn's `toolCallCount` (turn-scoped — `turnToolCallCount` **reused**, never a second counter, so the BANANA fail-open cannot return by a side door); the `completion` metadata of the assistant message **that ENDED the turn** — deliberately not `finalAssistantText`'s message, which by definition does not exist here; and a **census of the turn's assistant PART TYPES**. **Every field is opencode's own, read from the `AssistantMessage` schema the running serve publishes at `GET /doc`** (probed 1.18.18, and re-probed independently before merge): `cost` and `tokens` are **required**, `tokens` is `additionalProperties:false` over `input`/`output`/`reasoning`/`cache` required plus `total` optional, and **`finish` is optional and constrained by NO enum** — so it is carried as an opaque string and no vocabulary is asserted about it. **An absent number is reported ABSENT, never rendered as zero:** a genuine zero-token completion is the evidence this feature exists to surface, and must stay distinguishable from one nobody recorded. **The census exists because the extractor and the provider can disagree about where the answer went.** `finalAssistantText` reconstructs from `type === "text"` parts only, while opencode's `Part` union has **12 members** including `ReasoningPart`, **which carries its own `text` of identical type**. A turn whose visible output arrived as reasoning therefore reconstructs to `""` and is refused here **while opencode's own TUI renders it as a full answer** — which is exactly the contrast issue #168 reports, with no provider-side failure required. **The discriminator to read first is output tokens beside an empty answer:** `output > 0` means the provider emitted and this code did not read it, and `partTypes` names where it went; `output === 0` means the provider emitted nothing. **Nothing about WHICH turns are refused changes** — same predicate, same kinds, the provider's own `info.error` still quoted first. **Root cause remains OPEN** (see the live-probe recipe on issue #168); this makes the next occurrence self-diagnosing from the receipts alone, with no credentials required.
  - **WRITE PATH: THE CARVE-OUT NARROWS, IT DOES NOT CLOSE.** `guild_delegate` still does **not** set `requireAnswer` — its answer is the PATCH, so an empty report beside real edits is a successful delegation. It refuses only when **nothing was produced on any channel**: an empty (trimmed) report **AND** a turn that made **no tool calls** (`toolCallCount === 0`, turn-scoped — `turnToolCallCount`), with `capture.filesChanged === 0` as a pure guard so a refusal can never be returned while a patch with real content sits on disk. That is `empty-delegation`, exit-analogue null, `isError:true`, quoting the turn's own whitelisted `info.error` when opencode carried one — reaching the tool on `AskResult.providerError`, because this path never raises `EmptyAnswerError`. The full `capture`, `scaffoldWarning` included, rides out on the failure and the message says so.
  - **THE DECIDING COLUMN IS THE TURN'S, NOT THE CAPTURE'S — and that is the correction, not the design's first instinct.** The 2026-08-02 cut asked whether the capture had *completed* and found nothing. `ignoredFingerprint` marks a capture INCOMPLETE (`ignored-state-incomplete`) whenever `git status --ignored` collapses a **wholly-ignored directory** into one `!! dir/` entry, which it does for any such directory — `node_modules/` included — so that predicate was **unreachable in most real repositories**, this one among them. Probed 2026-08-03 on a fixture repo with one ignored dir: the rejected turn came back `ok:true`, `report:""` — issue #121's exact defect, unfixed. The same probe showed `patchPath` is **non-null** there (an empty patch file is written; only `nothingToReview` nulls the path), so a `patchPath === null` column would have blocked the refusal by a second route; it is deliberately absent, and it could never have served as a guard in any case, since `patchPath === null` holds only where `filesChanged === 0` already does. **Zero tool calls settles the question without consulting the capture at all: a model that reached for no tool cannot have edited a file or run a command**, so capture representability, logging state and crash state are orthogonal to it.
  - **THREE SHAPES STAY SUCCESSES, all live.** An empty report beside a real patch (pinned since PR #120 — a terse model that edited files has delivered). A **non-empty report beside zero edits** (probed 2026-08-02 — a model whose gated writes the approval bridge rejected returns a real report explaining why nothing was written). And a **silent tool-using turn** — the model ran commands, changed nothing and said nothing. That last is the **stated residual**: a turn with ≥1 tool call, an empty report and no captured change stays a success deliberately, because it *did* something; `structuredContent.activity` and the approval record are where that shows — and in that one preserved case the MCP **text block is still `""`**, so issue #121's literal symptom survives there by design, with the driver reading the structured surface rather than the text channel.
  - **STATED DEPENDENCY, and it belongs on the re-run-after-an-opencode-bump list.** This is the **first production consumer of the HISTORY payload's `part.type === "tool"` shape** — until now only the activity layer's SSE events and the offline fake asserted it (probed on opencode 1.18.5/1.18.7). If a bump renames or restructures that part type, `toolCallCount` reads 0 for every turn, and the silent-tool-using success above becomes a **false refusal** — **while the offline suite stays green**, because the fake serves the shape this repo wrote. Re-probe it alongside the `verify-guild-*.sh` proofs after an opencode bump that matters.
  - **CONSEQUENCE OF THE REDESIGN: the refusal now fires where the capture could not measure** — `GUILD_LOG=off`, a **crashed** capture, `ignored-state-incomplete`, and outside a git worktree — because in each of those a zero-tool-call turn is refusable regardless. So "loud on every model-calling tool" is now true without qualification, and the 2026-08-02 residual list dissolves. **A scaffolding-only turn is included:** opencode materializes its plugin runtime on the first request that loads it, so even a provider-rejected turn leaves scaffolding behind; that combination is refused, and the `scaffoldChanged`/`scaffoldWarning` tamper signal rides out on the failure, which is now the surface carrying it. **Exempting `scaffoldChanged:true` from the refusal was proposed in review and REJECTED** — scaffolding appears on precisely the turns this catches, so the exemption would disable the refusal in the common case, and the signal is not buried.
  - **ORDERING, AND WHAT IT COSTS.** The refusal is evaluated after the capture because the `filesChanged` guard reads it; the deciding half is a fact about the turn. Nothing about the snapshot, the patch decision, the recovery hint, the scaffold tamper signal or the `delegate-diff` bookkeeping moves. **The receipts record the TURN, which succeeded:** `completed` is written by the shared spine before the judgement exists, so it keeps `exit_code:0` / `capture_state:"complete"` / the byte-exact blank `raw_response` — deliberately unlike the read-path row above, where the failure *is* the turn's and is thrown from inside `askViaAgent`. That loses nothing in the log: PR #120's success writes a `delegate-diff` entry and this refusal's usual shape writes none, so `calls.jsonl` alone separates them (the one refusal that also writes one is a crashed capture, via the patch-less `diffUncaptured`, which carries `capture_complete:false`). The run verifies clean either way. **No session row is added to `askViaAgent`'s deletion matrix:** `guild_delegate` takes no `sessionId`, mints a fresh session per call and never keeps it, so the session is already deleted under `created here + success + !keepSession` before this line runs.
  - **EVIDENCE FOR THE DECISION:** a typo'd model id already answers HTTP 500 mid-turn and lands as `call-failed`, so it was never the gap; the gap is a **listed** id the provider rejects, which ends the turn with no text and no tool calls (2026-08-02). An approval-rejection run returns a **non-empty report with ≥1 tool call** and zero edits, and stays a success (2026-08-02). The ignored-directory unreachability and the non-null `patchPath` are probed (2026-08-03). **Provenance:** the defect was found by a Claude subagent testing PR #120 and confirmed by the coordinating Claude; option 3 is the **maintainer's** decision of 2026-08-02; the unreachability and the silent-bash false refusal came from an **adversarial Opus review with live probes and mutation testing** plus a **gemini-3.1-pro guild review**; the tool-call-count predicate is the **coordinator's** decision of 2026-08-03; dropping `patchPath === null` from it is the implementer's, on the probe evidence above. **Ratified by the maintainer, 2026-08-03** (the tool-call predicate and the `patchPath` omission both).

### Live activity layer (issue #20 — `src/activity.ts`, `test/activity.test.ts`)

> **Adjacent to the evidence layer, deliberately NOT part of it.** The items above specify the
> *receipts* — the model's own words, byte-exact, captured for later audit. The items below
> specify *liveness* — a normalized trace of what the model is DOING, while it does it. Mixing
> the two would let a visibility feature weaken an audit guarantee; C59 is the fence.

- **C59** — Live activity is recorded to `<runDir>/activity.jsonl`, a **separate file**. No `calls.jsonl` entry shape changes, no entry references it, and `verify()` never reads it — so an activity file (present, absent, or corrupt) can neither pass nor fail an evidence run. Honest-guarantee statements are part of the contract here as they are in area G: these lines are **opencode's report of the model's ACTIONS at opencode's fidelity**, not receipts of its words (that is `raw_response`), and **not containment** — surfacing a `bash` call does not gate it, and the human diff review remains the write path's review point (C49, C52).
- **C60** — The normalizer's source is the **probed** event set (opencode 1.18.5, 2026-07-27), not the documented union: `message.part.updated` with `part.type === "tool"` is primary — its `state.status` walks `pending` → `running` → `completed`|`error`, carrying `tool`, `callID`, `input`, `output`, `metadata.exit` — plus `file.edited`, `permission.asked`/`.replied`, `session.idle`, `session.error`, and the delta stream. A tool invocation is **counted exactly once** — `toolCalls` and `byTool` increment on the first `tool-called` seen for a `callID` and never again, whatever the stream does afterwards — and the recorded LINES are deduped by **suppressing a repeat of the state already recorded** for that `callID`, which is what collapses opencode's repeated `message.part.updated` bursts into one `tool-called` plus one terminal line on every probed sequence. That is a suppression rule, **not a guarantee of one terminal line per invocation**: a stream that reported two *different* terminal states for one `callID` (`completed` then `error`), or that returned to `running` after a terminal state, would record each state change, because the recorder trusts opencode's state machine rather than second-guessing it. Where the dedupe key degrades it degrades toward under-counting, never inflation: with `callID` absent the key falls back to session+tool, so two concurrent invocations of the same tool in one session collapse into one. `pending` is **not** a `tool-called`: it carries `input: {}`, so treating it as one made the dedupe keep the inputless event and hid every shell command until after it ran — the trace is emitted on `running`, which carries the real input. **An event with no `sessionID` is broadcast to every subscriber of that serve child and flagged `unattributed`, never dropped**: `file.edited` carries only `{file}`, and routing it by session left `filesEdited` permanently empty against a real opencode. **Unknown event types are dropped silently** (an opencode bump must never break a call); a missing expected event degrades visibility only. Deltas are the stream's bulk and are recorded **only at `full` detail**.
- **C61** — **A stream failure is never a call failure** (C31's posture, extended). One `GET /event` subscription per serve child, refcounted and routed by `sessionID` — so a panel's concurrent members cost one stream, and an event that **carries** a session id reaches only that member. **The routing is not total, and C60 says why:** an event with no `sessionID` (`file.edited` is the probed case) is broadcast to every subscriber on that serve child, flagged `unattributed`, because routing it by session dropped all of them. So concurrent members on one serve child **do** see each other's session-less events, marked as unattributed — the ambiguity C63 reports rather than hides. Reconnecting with backoff, and closed when the serve child dies. If it cannot attach or drops mid-turn, the call proceeds and the result carries `activity.degraded` with a reason — **a quiet activity list must never be mistaken for a quiet model**.
- **C62** — Knobs resolve **env > `modelguild.conf.local` > default** (C35's order): `GUILD_ACTIVITY` default **on** (capability-neutral — it removes nothing and gates nothing; `off` opens no subscription, writes no file, attaches no summary; only the literal `off` disables, so a typo fails toward recording), `GUILD_ACTIVITY_DETAIL` default **`summary`** (tool name + truncated input) with `full` additionally recording raw event properties — which can carry tool OUTPUT, i.e. file contents the model read: **the same sensitivity class as `GUILD_LOG_PROMPTS=full` (C26), and the conf template must say so**. `GUILD_LOG=off` mints no run dir and therefore writes no activity file: turning the record off must not leave a different record on disk.
- **C63** — Two surfaces, both **conditional on the layer being on** (C62): with `GUILD_ACTIVITY=off` — or `GUILD_LOG=off`, which mints no run dir — no subscription is opened, no `activity.jsonl` is written, and no summary is attached, so there is nothing for either surface to carry. With the layer on (the default), neither surface has to be asked for. (1) Every model-calling tool result carries a **bounded** `structuredContent.activity`: counts by tool, every `file.edited` path, failed tools and session errors, the first N action lines with inputs truncated, plus `truncated`, `degraded`, and the file path (null until something is actually written, so a path always names a file that exists). **`filesEdited` carries a stated ambiguity, not a silent one:** because `file.edited` has no session id (C60), a path is exact for a single call and — where two write-capable calls share one serve child — attributable only to the child, not the member. A panel reports it **per member**, never merged — a merged blob would lose which model did what. (2) `modelguild watch [--run ID] [--dir D] [--no-follow] [--json]` tails the newest run's activity live, re-resolving `latest` so it can be started **before** the call exists. It is the primary channel precisely because it depends on nothing but a file and a terminal.
- **C64** — MCP `notifications/progress` are emitted **only** when the client sent a `progressToken` in the call's `_meta` (unsolicited progress is forbidden by the protocol), are throttled to at most one per second — an event arriving inside a window REPLACES the one being held and the held one is flushed when the window closes, so the developer always sees the most recent action rather than a stale one. **Intermediate events inside a window are not sent on this channel**; the complete record is `activity.jsonl`, and the progress stream is a sampled view of it, not a copy. They also heartbeat through a quiet stretch — which is what keeps a legitimately slow turn from tripping the client's MCP idle watchdog. The tool RESULT is byte-identical with or without them: this channel is strictly additive to the file and the summary, and it never carries an approval or a decision.

### Approval bridge (issue #20 slice 4 — `src/approve.ts`, `test/approve.test.ts`)

> **Opt-in, default OFF, and adjacent to the visibility layer rather than part of it.** C59–C64
> specify *seeing*; C65–C69 specify *answering*. The honest-guarantee discipline of area G
> applies here in full: this is attention and an interrupt, never containment.

- **C65** — **Default OFF.** `GUILD_APPROVE` ∈ {`off` (default), `write`, `all`} and `GUILD_APPROVE_EGRESS` ∈ {`off` (default), `ask`} resolve **env > `modelguild.conf.local` > default** (C35's order). `write` gates `edit`/`write`/`patch`; `all` additionally gates `bash`; `ask` on the egress knob gates `webfetch`/`websearch`. **An unrecognized value is an ERROR that refuses the call**, never a silent `off` — the opposite of `GUILD_ACTIVITY` (C62) and deliberately so: a typo failing quiet would leave the developer believing edits are gated when they are not. `GUILD_APPROVE_TIMEOUT_MS` (default 120000) is lenient by the same logic — an unusable value falls back, and the outcome is fail-closed either way.
- **C66** — **The bridge may only NARROW, twice over, and both are asserted mechanically at TWO points each.** (1) It emits **only `action: "ask"`** rules — never `allow`. (2) It gates **only tools the agent def in force already allows**, because `ask` on a *denied* tool converts it into an approvable one. Both are checked when the ruleset is built **and** again at the wire boundary in `createSession`, so no caller can route around the bridge. Both are load-bearing because a `POST /session` ruleset **merges** over the agent def's resolved array last-match-wins (probe P2): the single rule `[{bash,*,allow}]` demonstrably gave the read-only `guild-read` agent a **working shell**. **The allow-set is READ FROM THE DEF IN FORCE, never from a table** (amended 2026-07-28, review finding H3): the first implementation carried a hard-coded mirror of the three shipped defs and never compared it to the def actually resolved, so a user-hardened `guild-build.md` that denied `bash` still got `{bash,*,ask}` emitted — a user-denied tool turned approvable. A mirror cannot hold this invariant; only the def can. A def that cannot be found or **parsed** is a **refusal**, not a guess. Consequence, pinned: `GUILD_APPROVE` alone gates **nothing** on the read paths. The hardened agent defs are **unmodified** (C47/C48 unaffected).
- **C67** — **Armed with no channel that can answer ⇒ REFUSE up front**, before `log.expect()` and before the write path's worktree snapshot (C24 gap parity, C37 ordering): a refused call logs nothing and touches nothing. This is required, not defensive: under `opencode serve` an unanswered `ask` **hangs** the turn until `GUILD_MESSAGE_TIMEOUT_MS` — it does **not** auto-reject (probe P3 refutes the bash-era claim) — so arming blind deadlocks. A channel is either an MCP client advertising `elicitation`, or a live `modelguild watch --approve` (a heartbeat presence file under `<logDir>/watchers/`, fresh within 20s, `mode:"approve"`; a plain watcher does not count because it cannot prompt). `GUILD_LOG=off` removes the watch channel by construction — no run dir, nowhere to publish a request.
- **C68** — **Fail-closed, arbitrated by opencode, and counted from evidence.** An unanswered request is **rejected** after `GUILD_APPROVE_TIMEOUT_MS`; the reject carries a **message that reaches the model verbatim**, and the turn **continues** (a rejection is a denied tool call, not an abort). A bridge that loses the event stream while armed rejects everything still open and reports `approval.degraded` — so a quiet `requests: 0` under `degraded` means "we went blind", never "the model asked for nothing"; the turn is **not** killed. **The approval clock only ever starts on a request the bridge actually SAW** (issue #77). **On re-attach the bridge RE-LISTS opencode's open requests** (issue #91): `GET /permission`, filtered to the bridge's **own** session, ids it has already settled or already replied to are skipped, and the rest are routed as if their events had arrived, with the approval clock starting when they are *seen*. **The guarantee is "recovered on re-attach", never "never missed":** the endpoint is a snapshot of what is still OPEN, so a request raised *and* settled by another answerer during the window leaves nothing behind to recover. A failed or malformed `GET /permission` is a degradation, never a call failure — nothing in the re-list path may throw into a tool call, and it decides nothing, so both C66 invariants are untouched by it. **Two further bounds.** (1) A request this bridge already replied to whose reply did **not** take effect — opencode still lists it open — is **recorded and not retried**, keeps the bridge degraded, falls back to the turn's own backstop, and is counted under `unsettled`. (2) The re-list is the **approval** layer only, so **`activity.degraded` still latches for the whole call** (C63). Nothing runs ungated in any of these cases — opencode holds the gated call at its `ask` (C67's probe P3) — so the cost of the window is a **stall**, not an unprompted tool. **`approval.degraded` means "blind NOW", and it can CLEAR** — only on evidence: the bus re-attached, the list answered, everything it named for this session was routed, and nothing this bridge had already decided is still sitting open. What is latched instead is **`blindWindows`**, the honest caveat on every other counter: `degraded: false` with `blindWindows > 0` means the stream dropped and recovered, and that the counts for that window can under-report. `blindWindows` counts distinct periods of **unrecovered** blindness, **not** socket drops. `recovered` names how many requests came back through the re-list rather than the stream. Approvals use the session-scoped reply endpoint, rejections the message-carrying one; `always` is never sent by the bridge. **Arbitration:** both channels may answer and **first reply wins, with opencode as arbiter** (a reply to a settled id is a 404) — but elicitation must not *pre-empt* the other channel. A cancel therefore **abstains** while another live channel could still answer, and **rejects** when elicitation is the sole channel; an explicit *decline* always settles. **Counters report only what opencode accepted** — a reply opencode refused, or one that never left the process (`undelivered`), is not a decision, and `decidedBy` names the settler of each one. **A REFUSED REPLY IS CHECKED, not classified from its status code** (issue #97): **any non-2xx status opencode actually returns** is followed by one bounded `GET /permission` — the **same v1 snapshot the re-list reads, filtered to the bridge's own session**. **No status is excluded.** **Still open ⇒ the decision was not delivered**, counted under **`unsettled`** and carrying an **`unsettledReason`**. **Not open ⇒ the cause is named only where the status supports it:** a **404** is **`contested`**, and **any other status is `refused`**, which records what was observed and diagnoses nothing. **The check unable to answer — it failed, or the list carried an entry that could not be attributed and might be this one — ⇒ neither claim**: the request is still counted in whichever of those two the status selects, but recorded `still_open: null`. **`undelivered` is the transport arm alone** — nothing came back, no status to reason from — and the check is deliberately *not* run there. Every record carries the real `http_status` (`null` on the transport arm) and the bucket it was counted in. The check is **not restricted to the approve path**, it **decides and sends nothing**, and **it may never throw into a tool call**: every failure answers "could not tell". **Three properties of these counters.** (a) **`unsettled` is not refusal-only, and a reply still IN FLIGHT is not one.** It is fed by two observations of one condition, keyed by permission id: this re-check, and the re-list's own discovery that a request the bridge already answered is still open — which fires even after a **2xx** opencode accepted that did not take effect. The gate is "the attempt returned"; an in-flight request is recorded, counted nowhere, and does not keep the bridge degraded. (b) **The four buckets are DISJOINT and never sum past `requests`** — one classification per request id, a later observation superseding an earlier weaker one, `unsettled` being the only positively-confirmed one and therefore terminal. (c) **`unsettledReason` keeps the most informative reason, not the first** — an approve-path refusal outranks any other refusal, which outranks the re-list arm. So the C69a expiry condition is readable out of it **whenever an approve-path confirmation has happened at all**, and where none has, the field says what was seen rather than manufacturing a diagnosis; per-request detail is in `approvals.jsonl` either way. **What the check cannot see:** it reads the **v1** open-request list, so a request raised on a different permission surface is absent from it and reads `still_open: false` — true of that list, and not the same claim as "somebody else answered". **And it has a PANEL cost:** `unattributable` entries are deliberately not session-scoped, so on a panel sharing one serve child a **sibling member's** protocol-violating entry downgrades this check to unconfirmed for everybody. Accepted rather than fixed; the `still_open: null` record names the condition. **A refused reply deliberately does NOT feed `approval.degraded`.** The condition is surfaced by `unsettled`/`unsettledReason` and by the `not-delivered` record's `still_open` field instead, with **no threshold**. A confirmed-undelivered **approval** is also deliberately **not** retried as a *reject* down the endpoint that still works. This bridge fails closed on **silence**, never on a delivered decision. **The elicitation prompt is an EMPTY-FIELD form:** `requestedSchema` declares no properties, so the client's own **Accept/Decline** buttons are the decision in one keypress. All request context (command, model, tool, sanitized detail, the bash-approves-a-shell note, the edit-covers-write/patch note, the deadline) therefore lives in the **message**. **Only the literal action `"accept"` approves**; `"decline"` rejects, and *everything else* — `"cancel"`, an unknown action, a malformed result, a transport failure — is treated as a cancel and never as consent. **No tool input grants approval**, and the test asserts no tool's input schema carries such a field. Requests and decisions are recorded to `<runDir>/approvals.jsonl`, a **separate file** that `verify()` never reads (C59's fence, extended).
- **C69** — **A per-session ruleset is fixed at CREATION, and the session is CHECKED, not assumed.** opencode echoes the stored ruleset on session create and on `GET /session/{id}`; before the turn is sent, the bridge verifies that ruleset both **carries every required `ask` rule** and **carries nothing that widens the agent** (amended 2026-07-28, review finding M7: a subset check alone passed a continued session whose ruleset also held `{bash,*,allow}`, so the turn ran on a widened session while reporting itself armed). What that check does **not** claim: it inspects the session record, not opencode's resolved behaviour — proving the latter is `verify-guild-*.sh`'s job against a live serve. A build that ignored the field, a **continued** session created before the knob was armed, or a widened session all yield an `approval-not-applied` refusal with **no model turn**; running ungated while the caller believes the turn is gated is the one outcome this feature must never produce. A session created *with* the rules continues normally (the workshop round-1 → round-2 flow is unaffected). **Honest bound, part of the contract:** an approved `bash` is an approved **shell** — it can read credentials, reach the network, and spawn processes that raise no further requests. This is visibility and an interrupt; the human **diff review** remains the write path's boundary (C49, C52). One further honesty item, probed: opencode's `write` permission key is **inert** on 1.18.7 — `edit` gates the write/patch family — so `edit` must never be dropped from a tier, and the result reports the **effective** tool family alongside the literal keys.
- **C69a** (added 2026-07-29, issue #93 — numbered as a suffix so no existing C-number shifts) — **The bridge speaks opencode's v1 permission surface, and it is pinned there BY DECISION.** C66 governs what a ruleset may say and C69 governs proving it was stored; this clause fixes *which API expresses and evaluates it*, because on 1.18.7 those are not the same question. The pinned seam is the ruleset on `POST /session`, its echo on `GET /session/{id}`, the open-request snapshot `GET /permission`, and the two replies (`POST /session/{id}/permissions/{permId}` to approve, `POST /permission/{id}/reply` to reject) — the pair the server's bridge and the `modelguild watch --approve` terminal both answer, which is also what makes C68's first-reply-wins arbitration work. opencode 1.18.7 additionally exposes a **v2** surface (`POST`/`GET /api/session/{id}/permission`), and **the v2 evaluator does not consult the v1 ruleset stored on the session**. **Migrating onto it would therefore produce a turn that reports itself armed while the gated tool runs — precisely the outcome C69 forbids — and would fail SILENTLY** (the probe, the further findings and the correction history are recorded in the `V1 PIN` block in `src/client.ts`). One consequence belongs in *this* clause, because it bounds what C68's detection is evidence of: the list it consults is the **v1** one, so a **v2**-issued request id — which lives in the other store — reads as *absent* and lands in `contested`, not `unsettled`. **The half-migration described above is therefore still caught by C69's stored-ruleset check and by this pin, never by that counter.** **Revisiting has PRECONDITIONS, and reading this clause does not satisfy them:** a *fresh* probe on a *later* opencode showing that the v2 evaluator honours the stored ruleset, or the ruleset re-expressed in v2's own terms with C66's two invariants re-derived for that shape; **and** the `verify-guild-*.sh`-style live proof extended to cover it. **That first precondition is RUNNABLE, not prose:** `modelguild/verify-permission-surface.sh` — local-only, opencode-required, no model called — is **differential**: it evaluates the same action against a gated session and a control session on the same agent and judges whether the ruleset changed the answer. It covers **every tool family the bridge can arm** — bash/edit/patch on `guild-build`, webfetch/websearch on `guild-read` — and its PIN-HOLDS banner names the pairs it actually evaluated rather than claiming the surface at large. **It refuses to conclude** when the stored ruleset is not *structurally* intact (an exact `{permission, pattern:"*", action:"ask"}` entry), when a control shows the agent never allowed the tool, when both creates return the same session id, or when the rounds for **either** session disagree: an unstable sample is not a verdict in either direction. It reports the pin holding (exit 0), *ATTENTION — revisit #93* (exit 7), or INCONCLUSIVE (exit 6, distinct on purpose: "could not check" must never read as "pin holds"). **The pin also carries an EXPIRY CONDITION:** `POST /session/{sessionID}/permissions/{permissionID}` (`permission.respond`, the approve half) is marked **`deprecated: true`** in 1.18.7's `/doc` — the **only** deprecated operation in the whole document. That is not an argument for migrating (a deprecated endpoint that works beats a live one that gates nothing), but when it is removed, approvals will fail while rejections still land; C68's counters **detect** that rather than hiding it, and the response is to move the **approve reply** to whatever opencode offers then, re-verify with the probe script, and update this clause and the in-code record together — **not** to read that removal as licence to move the ruleset itself onto v2, which this clause answers on its own evidence. The mechanical half is `modelguild/tests/check-v1-permission-pin.sh` (CI): no **literal** v2 *permission* path (`/api/permission…`, `/api/session/…/permission`) may appear in a non-comment line of `src/`, and the in-code record must still be there. It is scoped to the permission surface on purpose; being a literal grep it does **not** catch the same prefix split or computed (`"/api" + "/permission"`, a `const V2 = "/api"` template, a `join("/")`, or `/api` folded into the base URL), nor a *new* permission surface under some other prefix. Those stay review judgment, against this clause.

## E. Write-path semantics (`--edit` / `guild-build`, `build`)
*Oracle: `modelguild/ask.sh` (`_snapshot_tree`, `record_delegate_diff`, `_ignored_fingerprint`, `_submodule_state`); `run-tests.sh` cases 3c and delegate-diff block.*

> **WHICH TREE C36–C40 ARE ABOUT: the ONE root C71 resolves.** Every clause below says "the worktree"; since issue #107 that is the project dir *or* a validated sibling git worktree, resolved once per call and shared by the serve child and the capture. Read each of C37–C40 with that root substituted — the guarantees are unchanged, the directory they are asserted about is not always the project dir.

- **C36** — **No clean-worktree requirement** (the old exit-6 refusal is removed). A dirty tree proceeds.
- **C37** (amended 2026-07-29, issue #108 — the serve scaffolding leaves the STAGING too) — Before the model runs, the worktree is snapshotted as a git tree via `git write-tree` against a **throwaway index** (`git add -A`, honoring `.gitignore`) — no effect on the caller's real index or worktree. **`opencode serve`'s own runtime scaffolding is then dropped from that index wherever the repository does not track it**: the `.opencode/node_modules/**` tree and the `.opencode/{package.json,package-lock.json,bun.lock,.gitignore}` manifests — **exactly the `isServeScaffold` set the ignored-file fingerprint has excluded since 2026-07-22**, applied to the staging so the two are consistent rather than a second exclusion being invented. **`.gitignore` alone did NOT hold this line, and the reason is a probed opencode behaviour** (C70): opencode writes an `.opencode/.gitignore` covering everything it materializes, but **does not overwrite an existing one**, so a repo that COMMITS its own never receives opencode's — the scaffolding is untracked-but-not-ignored, `git add -A` stages every path of it, and the patch a human is meant to review fills with thousands of paths it did not ask for. That reaches the patch only when the scaffolding appears **between** the two snapshots (otherwise it is in both trees and cancels in `diff-tree`), i.e. the first delegation into a tree whose runtime is not yet materialized — **once per new worktree since C71/#107**, which under a worktree-per-issue workflow is once per issue. **Scoped to untracked, deliberately:** pollution is by definition newly created files, and a repo that commits `.opencode/package.json` owns it — the model's edit to that file stays in the diff like any other tracked file. **ONE CODE PATH IS NOT ONE RESULT — THE TRACKED SET IS PINNED AT THE BASELINE AND CARRIED.** Both snapshots going through `snapshotTree` is necessary and was **not** sufficient: the predicate's second input (*is this path tracked?*) was read from HEAD once per snapshot, and `guild-build` allows `bash`, so a delegated model running `git add -A && git commit` moves HEAD mid-turn — scaffolding present at baseline was then dropped from the before-tree and kept in the after-tree, appearing in the patch as **files the model never created**, with `git rm --cached` + commit the mirror (a deletion recorded for a file still on disk). Both were reproduced. So `WorktreeSnapshot.trackedScaffold` resolves the set **once, at the baseline**, and `CaptureInput` carries it into the after-snapshot; the question asked is *did the repository own this file BEFORE the turn*, which has exactly one answer per call. An **unborn** HEAD is "nothing tracked" (drop everything); a `ls-tree` that **fails** excludes nothing, i.e. the pre-#108 noisy patch, because dropping a path the repo genuinely tracks would record a deletion that did not happen while still reporting `captureComplete:true`. **Cost, stated, and the pairing is part of it:** a write into the untracked scaffolding is now absent from the patch in **every** repo, where a repo with a committed ignore file used to show it by accident — and the directory in question is one `opencode serve` loads and executes. `scaffoldDigest`'s before/after tamper signal (C71/#107) is untouched, still fires, and is the only remaining surface; but in the common shape — a plugin written on the first delegation into a tree that already had `.opencode/` — C71's three-case wording correctly selects the **benign** branch, so the patch shows nothing and the surviving signal reads as opencode's housekeeping. That wording is not changed (it is right in isolation); the pairing is stated wherever the cost is, and the warning is to be read as a prompt to inspect the directory rather than an all-clear. `.opencode/agent/**` is **not** in the set and stays fully in the fingerprint and the patch: a bash-capable model tampering with a hardened def is precisely what capture must never drop. *Provenance: maintainer decision (Ben, 2026-07-29, issue #108) — exclude from the snapshot rather than warn, refuse, or document; the untracked scoping is his too. The baseline pinning is Claude's, after review found the per-snapshot re-derivation.*
- **C38** (amended 2026-07-29, issue #108) — The recorded patch is the **model's changes only** on a dirty tree, **including created files** (a `git diff <sha>` that misses created files is a contract violation). It lands at `modelguild/logs/<run_id>/diff-<call_id>.patch` and is logged as a `delegate-diff` entry. **It never carries the serve scaffolding**, whatever the target repo's ignore rules say — that is C37's staging exclusion, and it is what makes this artifact reviewable rather than reviewable-if-you-are-lucky.
- **C39** (amended 2026-07-29, issue #107) — Clobbered work is recoverable via `git checkout <tree> -- <path>`; the snapshot tree id is printed to stderr on a dirty tree. **When a `worktree` was targeted the hint is the `git -C <worktree> checkout <tree> -- <path>` form** — a worktree shares its main checkout's object database, so the bare form pasted in the wrong tree restores successfully into the wrong tree. An untargeted call's hint is the bare form, unchanged.
- **C40** (amended 2026-07-28, issue #74 — a crashed capture now leaves a record; and 2026-07-29, issue #108) — Capture is marked **incomplete** (and the log will fail integrity, surfaced loudly) when the baseline/after tree, ignored-file fingerprint, or submodule state cannot be faithfully represented; a run that changed no tracked files reports "nothing to review" and writes no patch. **C37's staging exclusion trips no incomplete reason and is not one:** the scaffolding is absent from both trees by construction, so the capture of everything else stays COMPLETE — and a turn whose only effect was a scaffolding write now reports "nothing to review" rather than a patch of it, with `scaffoldChanged` the surface that says otherwise. **A capture that THROWS (`capture-crashed`) is incomplete on the same terms and leaves the same durable record** — a patch-less `delegate-diff` (C29) with `capture_complete:false`, so the run fails integrity exactly as the representability reasons do. This was the gap: the crash path returned before reaching any log write, so the run held only its three lifecycle entries and **verified clean**, indistinguishable from a delegation that changed nothing — while `SECURITY.md` already told the reader such a run fails loudly. The log write is guarded per C31 (it may never mask the crash it records or sink the call), and the tool result keeps its existing fields unchanged: `captureComplete:false`, `incompleteReason:"capture-crashed"`, `patchPath:null`. **The residual is named rather than left implicit:** if the evidence write ITSELF fails — a lock timeout, or an ENOSPC *correlated* with whatever crashed the capture — no `delegate-diff` lands and the run **verifies clean again**, because no cardinality rule requires that entry (C24 governs only expected/started/completed, so `verify` has nothing to miss). `capture.recordFailed` is set on **both** write paths (the patch-ful `diff` and the patch-less `diffUncaptured`) and is the ONLY witness of that gap; it rides on the tool result precisely because the log whose absence it reports cannot carry it. The snapshot is a **record, not containment** — it does not run for read-only agents (case 3c).

## F. Command surface
*Oracle: `.claude/commands/guild/*.md` (8 files, was 9 pre-M12); `AGENTS.md` command list.*

- **C41** (amended 2026-07-23, issue #36 — `/guild:witness` retired at M12) — Exactly **eight** namespaced commands exist as `/guild:<name>`: `consult`, `panel`, `workshop`, `review`, `delegate`, `research`, `collaborate`, `configure` (the ninth, `witness`, retired with the automated witness at M12 — the set now matches `src/init.ts` `COMMAND_DOCS`). Files live in `.claude/commands/guild/` (the subdirectory is the namespace and is load-bearing against silent collisions).
- **C42** — Every model-touching command carries a **prompt-injection guard**: external model output is untrusted **data**, never instructions; directives aimed at Claude are surfaced as a finding, not executed.
- **C43** — Commands consult the model policy per call (via `ask.sh`), not independently; `panel`/`workshop` set one `$GUILD_RUN_ID` so the whole workflow is one auditable run.
- **C44** — Commands that spend multiple calls **state the call count before spending** (e.g. `panel` 2–3; `workshop` ~2/model). Normal `consult`/`review`/`research`/`collaborate` are one call; no silent retries.
- **C45** (`witness` part marked historical 2026-07-23, issue #36 — witness retired at M12) — Verify-not-relay contracts: `review` re-reads the changed files and marks each finding Confirmed/Refuted/Uncertain; `research` fetches each cited source (Confirmed/Refuted/Unsourced) and states verification coverage. (`witness` treated a report missing `call_id` citations or quoted `raw_response` as Inconclusive, with fixed prompt text not Claude's to soften — **retired at M12** with the automated witness; historical.) The `review`/`research` contracts are live.
- **C46** (amended 2026-07-28, issue #60 — the logging half was aspirational) — Anthropic voices come via a Claude **subagent** (Task/Agent tool). The `subagent-voice` entry type is part of the log API and stays specified (C29/C30), but **no production command or tool writes one today**: the commands that spawn a subagent voice (`panel`, `workshop`, `consult`) tell Claude to keep its words distinct in the synthesis and explicitly *not* to log it as a receipt, because a subagent turn would be Claude's own transcription (`claim:true, captured:false`) — testimony for the witness that was retired at M12, not the auto-captured `raw_response` the log exists to hold. The only callers of `EvidenceLog.subagentVoice` are the tests that pin the entry shape. Consequence, stated: a run whose Anthropic voice came from a subagent has **no receipt for that voice** — attribution lives in Claude's answer, not in `calls.jsonl`.

### Installed-payload state (issues #22, #94 — `src/init.ts`, `src/notice.ts`, `test/init.test.ts`, `test/doctor.test.ts`)

- **C72** (added 2026-07-29, issue #94; renumbered from C71 on 2026-07-29, issue #113) — **THE SERVER UPDATES ITSELF; THE PAYLOAD IN THE USER'S REPO DOES NOT, AND THAT DIVERGENCE IS REPORTED.** **Every installed payload file is classified into exactly one of THREE states, from three hashes** — `recorded` (what init wrote), `current` (what is on disk), `shipped` (what the RUNNING package holds): **drift** (C-less, issue #22 — `recorded ≠ current` and `recorded ≠ shipped`: edited *and* the release moved on), **skew** (new here — `recorded === current ≠ shipped`: untouched and simply behind), and **unjudgeable** (no ownership record, so it stays unjudged; **explicitly not reclassified as skew**). A fourth combination is deliberately *nothing*: edited against the version the release still ships, where there is nothing to catch up to. **"Shipped" means the payload of the package the executing code was loaded from** — derived from `import.meta.url`, and cannot be redirected by a cwd or an env var. **Mode-correctness is INHERITED, not reimplemented:** each file is located project-then-global (`--global` ⇒ global only) and judged against the record of the location it was found in, both routed through `payloadDest`/`recordPathFor`, so a `--global` install is judged by the global record. **ONE detection, three surfaces** (`scanInstalledPayload`): `doctor`, the `guild_status` tool (`structuredContent.payload`, carrying the deciding installed-vs-shipped hashes), and the MCP server's start-up notice. **REPORT ONLY, and never a failure:** no file is touched, no `--force` is offered, and skew — like drift — **does not change `doctor`'s exit code**. `init` reports drift but **never skew**. **The start-up notice is the only UNSOLICITED surface, and it is bounded three ways.** (1) It writes to **stderr only** — stdout is the MCP transport — as a single block. (2) It is suppressed **per server version and payload state, not per session**. **The state is never written into the user's repository** — `$GUILD_ROOT` when one is pinned, otherwise `<home>/.claude/modelguild/.modelguild-notice.json` — and deliberately not in the ownership record. It is keyed on the **ownership record(s) the skewed files were judged against**, and a **global** payload shared by many projects is announced once rather than once per project. **The suppression key is VERSION + PAYLOAD FINGERPRINT (issue #145, maintainer decision 2026-08-02)** — a digest over the SHIPPED-payload hashes `scanInstalledPayload` already computed for the scan's skewed files, folded in beside the version, no rescan. Either half failing to compute (an unreadable version, or a fingerprint that cannot be formed) suppresses **nothing**, same direction as before: a republish under one version, a moving dist-tag, or development from a source checkout now **re-fires** the notice instead of staying silent, because the shipped bytes moved even though the version did not. **Cost, accepted with the decision:** someone iterating on the payload from a source checkout sees the notice on every payload change, not just every version bump; `GUILD_PAYLOAD_NOTICE=off` remains the escape. The residual this closes is narrower than it sounds: the fingerprint detects that the *shipped* payload changed, not anything `doctor`/`guild_status` would not already say from the same scan — it is the unsolicited surface catching up to what the asked-for ones already report. **Backward compatibility:** a state file written before this change recorded the bare version string; read back with no fingerprint, which never matches a freshly computed one, so a legacy entry does not suppress the new key — the notice fires once more and the entry is rewritten with both fields (errs toward telling, this module's stated safe direction). (3) `GUILD_PAYLOAD_NOTICE` ∈ {`on` (default), `off`} resolves **env > `modelguild.conf.local` > default** across the layered roots (C9/C35), leniently — only the literal `off` disables, so a typo fails toward telling the user — and governs **the notice alone**: `doctor` and `guild_status` report skew with it off. `GUILD_LOG=off` is deliberately **not** a second silencer (issue #94). **It may never break the server** (C31's posture, extended to start-up): the emitter cannot throw, and — because a `try/catch` cannot reach a *block* — the state read is gated on `lstat` saying a regular file, so a FIFO at that path cannot hang start-up before `connect`. The state write goes through a temp file and `rename`, which is both atomic and what stops it following a symlink out of the guild root; `safeJoin`'s refuse-any-symlinked-component rule is deliberately **not** applied, because a dotfiles-managed `~/.claude` is a symlink in a setup this repo documents. (Since issue #156 that rule is **project mode's**, not init's everywhere — C77 — so this exemption now agrees with what `init --global` itself does.) **A persistently unwritable state means the notice fires on EVERY start, not once** — bound (2) is the one that gives way. **DIRECTION IS NOT CLAIMED, and the wording says so.** Two hashes cannot carry it and the ownership record holds no version — correct in the normal case (the server moves, the payload does not) and wrong for a user who pinned an older server on purpose. Every surface reports the files as **out of sync with what this server ships**, states the usual cause, and names the exception, rather than asserting a direction it cannot see. **The remedy is VERSION-PINNED for exactly that reason:** `npx modelguild@<running version> init`, not plain `npx modelguild init`, which resolves the **latest** dist-tag and therefore does *not* converge in the pinned-older-server case. Where the version cannot be read the notice says so and names what the unpinned command would do instead.
- **C77** (added 2026-08-05, issue #156; **AMENDED 2026-08-15 by C79 — four statements below are now FALSE and are kept only to show what changed:** the four routes it calls "reproduced and unrefused" are all now REFUSED with zero files written; "Project mode is deliberately not covered" no longer holds, since `assertRecordLinkWritable` covers both modes; "TWO ENUMERATED SHAPES … in `--global`" is now THREE (a symlink loop) in BOTH modes; and `.mcp.json` is no longer an unchanged `safeJoin` ancillary but part of `planFor`'s eager pass. **Also amended 2026-08-15 by C80:** the leaf-symlink skip described below is now settled by the ownership rule in `--global`) — **A SYMLINK AT AN INSTALL DESTINATION IS DECIDED BY MODE, and the leaf is decided by neither mode's path rule.** **Project mode is unchanged:** `safeJoin` refuses a symlink at any existing component of a destination, throwing `refusing destination symlink: <dest-rel>` — the target is somebody's source repository and a planted link must not redirect a write out of it. **`--global` FOLLOWS directory-component symlinks** (`globalJoin`: the `..`/absolute/empty-component guard is unchanged, the per-component refusal is not applied), because `--global` writes into the user's own config and a dotfiles manager makes `~/.claude` and `<xdg>/opencode` symlinks — under the old rule such a layout threw at the **first linked destination**, and what that cost depended on which link: with `~/.claude` linked the throw came from `planFor`'s **eager** resolution of the ownership record path, before the install loop ran at all, so **zero** files were installed; a layout linking only `<xdg>/opencode` got past that, installed the command docs and then threw on the first agent def — a **partial** install with no ownership record. **A LEAF payload-file symlink is neither followed nor refused in global mode:** it is routed into the pre-existing never-clobber branch — reported in `skipped`, warned by dest-rel, the remaining payload installed, the ownership record still written — so no partial, unrepairable install can result from one. That leaf rule is **global-only**: in project mode a **live** leaf link is still refused by `safeJoin`, by name. **The existence test that selects that branch is `lstat`-based** (`entryExists`), because `existsSync` follows a link and therefore called a **dangling** one absent, sending the write through it; that lands in project mode too, and it changes **two** observable behaviours there from the one cause — a dangling leaf link had let payload bytes land outside the target directory, and where the link's target directory did not exist the same write was a mid-loop `ENOENT` that aborted the install; both are now a clean skip. **THE OWNERSHIP RECORD'S OWN LEAF IS THE EXCEPTION, AND IT IS A WARNING RATHER THAN A SKIP** (maintainer decision 2026-08-05): `writeRecords` sits outside the install loop, so a symlink at the record path is **written through** rather than skipped. In global mode that holds for a live link and — with the two exceptions below — a dangling one; in project mode `safeJoin` still refuses a **live** record link (it is thrown from `planFor`, before anything is written), while a **dangling** one reaches the same write-through-and-warn. **TWO ENUMERATED SHAPES ARE REFUSED RATHER THAN WRITTEN THROUGH, BEFORE ANY BYTE IS INSTALLED:** in `--global`, a **dangling** record link whose *target's* directory **does not exist**, and one whose target's directory **is not a directory** (`assertRecordLinkWritable`, called from `planFor`'s eager resolution of the record path). `writeRecords` creates the LINK's directory and not the target's, so those writes raised a raw `ENOENT`/`ENOTDIR` **after the whole payload was already on disk** — an install with no ownership record, which a re-run reproduced identically and `--uninstall` could not repair, since it removes only what a record proves init wrote. Each refusal names the record path, the link's target and that directory, says which of the two conditions it is and what to do, and writes nothing. **They are two named conditions, deliberately NOT a general "can this write succeed?" predicate** (maintainer decision 2026-08-05): any other cause still fails from the write, and the stat is positive evidence only, so a stat that cannot be taken refuses nothing. **"Fails from the write" is not a mild outcome and must not be read as one:** the record write is the LAST step, so every such cause lands in exactly the unrepairable state the two enumerated refusals exist to prevent — payload on disk, no ownership record, a re-run reproducing it identically, and an `--uninstall` that can prove nothing is ours. Four routes are reproduced and unrefused: a **symlink loop** at the record path (`existsSync` reports false via `ELOOP`, so it reads as dangling while `dirname(target)` is a real directory — 13 files installed, no record); a **dangling directory link ABOVE** the record path, e.g. at `<home>/.claude/modelguild` (`lstat` on the record path itself throws `ENOENT` because its parent does not resolve, so nothing is refused — 11 files installed, no record); `<xdg>/opencode` pointing at a **regular file** (`ENOTDIR` from `ensureDir` mid-loop — 8 files, no record, where the pre-#156 rule at least refused by name); and the same **dangling record link in PROJECT mode**, which reaches it too. So the fence is drawn around the shape whose detection is cheap, not around the failure mode; **creating that directory was considered and REJECTED** — init making directories wherever a user's link happens to point is a bigger step than declining. It gates an **install** only: `--uninstall` writes no record, so a link no write could have followed must not block a removal. **Project mode is deliberately not covered** — there the same layout still throws from the write, as it did before this change and on `origin/main`. The asymmetry with a payload leaf is deliberate and has a reason: only init writes the record, so there is no user content it is expected to preserve, and a dotfiles manager that links files individually — GNU stow does, when the parent directory already exists — legitimately puts a link there. A user **can** nonetheless point that path at a file of their own, which is why it is not silent: `init` emits a warning naming **the link and the file the bytes actually landed in**, and saying whether the record replaced that file's contents or created it. **Costs, stated:** `--global` now writes wherever the user's own directory links point, including outside `$HOME`; uninstall's empty-directory prune reaches into the backing store the same way; **`--uninstall` unlinks a symlinked record and leaves the file it pointed at in place** (`existsSync` + `unlinkSync` on the link path — so a live record link is removed, a dangling one is left, and neither deletes the target); and nothing here bounds a wrong or hostile `~/.claude` link, which redirects the entire payload — the ownership record and hash-verified uninstall are what they always were, a freshness check on bytes, not an authorization check on paths. **Scope, stated:** this clause is about the payload destinations and the ownership record resolved by `planFor`. The project-only ancillary writes (`.gitignore`, `--write-mcp`'s `.mcp.json`) still go through `safeJoin` and are unchanged. Covered by `test/init.test.ts`.
- **C78** (added 2026-08-15, issues #162 and #163) — **EVERY FILE PATH THIS CODEBASE READS OR WRITES IS GATED ON ITS SHAPE FIRST, THROUGH ONE PREDICATE, AND THE PREDICATE IS `stat` NOT `lstat`.** `src/fsguard.ts` exports `isRegularFile` / `isNonRegularFile` and imports nothing but `node:fs`, so the installer, the config and policy resolvers, the evidence log and the CLI share it without a dependency cycle — the four sites that had four different answers cannot drift apart again. **WHY A GATE AND NOT A `try/catch`, which is the whole of #162: `existsSync` answers TRUE for a FIFO, and `readFileSync` *and* `writeFileSync` on a FIFO with no peer BLOCK. A block is not an exception, so no surrounding handler reaches it** — the only defence is to look at the path's shape before touching it. `src/notice.ts` reached this conclusion first and fixed it in one place (C72); this states it once for everywhere else. **BOTH DIRECTIONS ARE GATED, and the write direction is the one nobody had named:** `writeRecords` is the LAST step of the default install, so a FIFO there hung the process with the entire payload already on disk — the C77 failure state reached by a third route. `addGitignoreBlock` and `writeMcpJson` read *and* write, and are gated on both. **`stat`, NOT `lstat`, and that choice is the whole of #163:** `lstat` answers about the LINK, so a payload file symlinked by a dotfiles manager (GNU stow links leaves whenever the parent cannot be tree-folded; chezmoi's `symlink_` does it by design) read as "not a regular file" and vanished from skew and drift detection entirely — taking C72's three surfaces quiet with **no signal that it had**. Measured on a stow-style install of the 8 command docs: `existsSync` 8, `lstatSync().isFile()` **0**, `statSync().isFile()` 8. `stat` answers the question every runtime consumer actually asks, since `open(2)` follows links, while still rejecting a directory, FIFO, socket or device. **The same predicate replaces a bare `existsSync` at the agent-def pre-check** (`hardenedDefPresent`/`hardenedDefPresentIn`): a DIRECTORY at a def path satisfied `existsSync`, so the pre-check passed and the real failure surfaced seconds later mid-turn out of opencode instead of as the clean `agent-def-missing` refusal that check exists to give. Fail-CLOSED, so no working setup starts refusing; it follows symlinks exactly as opencode's own resolution does, so a linked def still counts. **A NON-REGULAR PATH READS AS ABSENT, never as an error** — the same answer the code already gave for a missing file — so the behaviour change is confined to paths that previously hung. **REACHABILITY, probed rather than reasoned:** with `$GUILD_CONF` pointed at a FIFO the MCP server never reaches `connect`, so Claude Code shows a server that never comes up; `claude mcp get modelguild` and `claude mcp list` both hang, because the CLI is launching our own server. An unregistered server name returns instantly, which is what isolates the cause to us rather than to the Claude CLI. The fix only takes effect for a user once they upgrade the package the CLI launches. **THE TEST DISCIPLINE IS PART OF THE CONTRACT, because the obvious test cannot work.** A regression here BLOCKS, and a suite that hangs hands CI a timeout with no signal — strictly worse than no test. So every FIFO assertion runs where the OS is the arbiter: `runBounded` (a whole CLI invocation) and `runBoundedProbe` (a library-level call) in `test/harness.ts` spawn a child under a hard wall-clock bound and `SIGKILL`, so a regression is a killed child and a red line in bounded time. **The reasoning that must not be re-derived:** "in-process is safe because post-fix nothing opens the FIFO" is true and irrelevant — the bite-check is a PRE-fix question, and pre-fix the code does open it and does block. Directory- and symlink-shaped cases stay in-process deliberately, since neither can block. **A killed child reports `status === null`, so `status !== 0` ACCEPTS a hang** — assertions on a bounded run must require a real non-zero status, or they pass against the very hang they exist to catch. Verified by reverting `src/` and running the full suite: 26 assertions fail across `policy`, `config`, `doctor` and `init`, and every suite completes. Covered by `test/init.test.ts`, `test/doctor.test.ts`, `test/config.test.ts` and `test/policy.test.ts`.
- **C79** (added 2026-08-15, issues #167/#159/#160/#161/#164) — **WHEN A REFUSAL LANDS IS PART OF THE CONTRACT, AND IT RUNS OPPOSITE BY DIRECTION.** Every path check used to run **lazily** — `destFor` from inside both loops, `.mcp.json` between the payload loop and `writeRecords`, `.gitignore` after both — so a failure landed **after** the filesystem was mutated; and because the ownership record is what `--uninstall` and never-clobber both key on, a run that died before `writeRecords` left an install **neither present nor absent and unrepairable by any later invocation**. **AN INSTALL REFUSES EARLY OR NOT AT ALL.** `planFor` resolves every payload destination, the ownership record and — under `--write-mcp` — `.mcp.json` **before** the payload loop, so a refusal writes nothing; past that point a per-file failure is a warning-and-skip, and the run reaches `writeRecords` with a record accounting for what landed. **ONE ANCILLARY WRITE ESCAPES THAT RULE AND IS A KNOWN DEFECT, NOT A DESIGN (issue #174):** `addGitignoreBlock` runs **after** `writeRecords` and opens with an unguarded `safeJoin`, which throws on a **live** leaf symlink at `.gitignore` — so the payload *and* the record land, and the run then exits non-zero saying `Nothing was installed`, permanently, on every re-run. An earlier version of this clause asserted the opposite ("past that point nothing may throw… the ancillary writes are guarded"); that was **false when written**, and the code comment at `src/init.ts:418-422` claiming the block skips-and-warns is false in the same way. The *dangling* case does skip and warn correctly, which is why the gap went unseen. Until #174 lands, read this rule as holding for every destination `planFor` validates and for the dangling-`.gitignore` case, and **not** for a live symlink there. **AN UNINSTALL NEVER REFUSES AT ALL** — its job is removal, so `removeMcpKey`/`stripGitignoreOnly` decline the write rather than fail the run, and a payload file that cannot be resolved, read or unlinked is one warning while the removal continues; otherwise an ancillary file holds the payload hostage. **The dividing principle: REFUSE what can be determined before writing** (a symlinked or non-regular destination, an unparseable `.mcp.json`) **and DEGRADE what can only be learnt by writing** (`EACCES`, `ENOSPC`, a race). An `accessSync`-style "can this write succeed?" predicate is deliberately **not** used — that is the general predicate C77 declined. **`safeJoin` gates on `lstat`, not `existsSync`**, which follows a link and therefore skipped a **dangling** component entirely; scoped to DIRECTORY components, because C77's leaf rule (a live link refused, a dangling one taking the never-clobber skip) is unchanged. **`assertRecordLinkWritable` gains a THIRD enumerated condition** — a record path that cannot be RESOLVED although its target's directory is fine (a symlink **loop**, which `existsSync` reports exactly as it reports a dangling link) — placed **after** the `ENOENT`/`ENOTDIR` conditions so their more specific wording still wins, and it now covers **both modes**: C77 scoped it to `--global` on a minimal-change argument, and project mode reached the identical unrepairable state. **`--global` destinations also get an eager DIRECTORY-CHAIN check** (`checkGlobalDirChain`), **install-only** — running it on `--uninstall` is the `wip/issue-156-full-review-work` regression, where a broken component in one destination tree aborts removal from the other. **`InitResult.blocked` DECIDES THE EXIT CODE (issue #164), and the test is MECHANICAL, not causal:** the destinations the run was expected to produce that do **not** resolve to a regular file once it is over. So a never-clobber skip stays exit **0** (the user's own file, or a live symlink to one, does resolve) while a dangling link, a directory in the way or a failed write exits **1**; `cli.ts` prints `INCOMPLETE` and names them. A shape init **declines** to write is policy, not `blocked`. That distinction is load-bearing rather than cosmetic: C16 makes an absent hardened agent def a **refusal** at every model-calling tool, so an install reporting success over a payload missing one is reporting a working setup that cannot answer. **Costs, stated:** an install can now refuse over a `.mcp.json` an earlier release limped past (`--write-mcp` is an explicit ask, and skipping it while printing "Installed" is a different lie); `--uninstall` can report success-with-warnings while leaving files behind, with the record removed, so a later `doctor` reports those leftovers as *unjudgeable*; and an uninstall whose `.mcp.json` write failed removes the record that proved the key was ours, so a second run keeps the key fail-safe — the alternative, keeping a record that claims files already gone, was judged worse. Covered by `test/init.test.ts`.
- **C80** (added 2026-08-15, issue #165, maintainer decision 2026-08-05) — **A LEAF PAYLOAD SYMLINK IS SETTLED BY THE OWNERSHIP RULE, NOT BY PATH TYPE — AND THE HASH THAT SETTLES IT IS A FRESHNESS CHECK, NOT AN AUTHORIZATION CHECK.** C77 covers a linked config **directory**; the per-**file** layout is the common one, because GNU stow folds a tree only when the parent is empty and `~/.claude` rarely is (`settings.json`, `projects/`, credentials stow did not create), and chezmoi's `symlink_` targets produce it deliberately. C77 skipped such a destination as a non-file, so **the user's copy stayed on the release that wrote it, permanently — while `doctor` classified it as C72 SKEW and named `npx modelguild init` as the fix**, advice that could not work (`installed=0`, `blocked=[]`, exit 0, store copy unchanged). Detection and remedy contradicted each other. **In `--global` a leaf link is now decided from the BYTES read THROUGH it:** ours ⇒ the write goes through the link, upgrading the dotfiles copy and leaving the link a link; not ours ⇒ the never-clobber skip, with the warning naming the link **and** its target; **dangling** ⇒ still a skip, **by the rule rather than by an exception to it**, having no bytes to match. Nothing new is invented — never-clobber always keyed on bytes — and the mechanical change is one predicate: `isRegularFile` (C78's `stat`) for `lstat().isFile()`, the same correction #163 made to the C72 scan for the same layout. **Scope is `--global` alone, inheriting C77's asymmetry unchanged:** project mode still refuses a live leaf link eagerly, **even when the bytes are ours**. **C79 is untouched in both directions and for opposite reasons:** a live link to a regular file resolves, so a never-clobber skip stays exit 0; a dangling one resolves to nothing, so that payload piece is genuinely missing and the run exits 1. **THE COST, STATED RATHER THAN LEFT IMPLIED BY THE WORD "OWNERSHIP":** recorded hashes are of **shipped** payload files, published in the npm tarball, so anyone able to plant the link can plant matching bytes behind it — demonstrated end to end (install v1, copy v1's `consult.md` to a victim path, point the destination at it, install v2 ⇒ the victim is overwritten, warning naming both paths). It proves the file is still the release init put there and **nothing about who placed the link**. Bounded by "only already-matching bytes are followed" and by planting-a-link implying code execution as the user — trusted-repo posture — and written into the runtime warning, `SECURITY.md` and `docs/setup.md`, with a test pinning the wording so it cannot be quietly reworded away. **A FURTHER BOUND: THE CHECK IS NOT ATOMIC WITH THE WRITE** — several independent resolutions of the destination rather than one held handle — so a link repointed in between redirects the bytes and can make even the warning name the wrong file. Documented in `SECURITY.md`, **not fixed**; the freshness check bounds a cooperative filesystem, not a racing one. **UNINSTALL REMOVES THE LINK AND LEAVES THE TARGET, NAMING IT** — `unlink(2)` never follows a final component, so this is what it already did; the change is that it is disclosed. Asymmetric with install on purpose: updating a file the user asked to be there is not the act of deleting one out of a store they manage. **Stated consequence:** install → uninstall → reinstall does **not** restore the stow layout — the reinstall writes an ordinary regular file and the orphan stays in the store. **THE RECORD LEAF DELIBERATELY KEEPS DIVERGING, AND THE DIVERGENCE IS FORCED:** there is no record **of** the record, so the hash a gate would compare against is the one that file is about to contain — circular, leaving only C77's write-through (which the stow layout needs) or skip-always (which breaks it). **Write-through stands unchanged; its DISCLOSURE is SPLIT, and the split is not tidiness.** *Which* link is present and *where it points* resolves at **plan time** (nothing in the run rewrites the link, and that is where a refusal would go if this is revisited); *whether the target exists* is read **beside the write**, because it is a fact about the TARGET and the payload loop can have just created it. Hoisting that second half was tried and was **wrong**: a record link aimed at a not-yet-installed payload file is **dangling at plan time and live by the write**, so the message announced a creation while overwriting an agent def the same run had installed — `origin/main`'s late call site said "replaced that file's contents" and was correct. That collision is now **named**, best-effort by resolved-path comparison: absolute, relative and `..`-bearing links match; a **chained** link and one reaching the def through a **directory** symlink fall back to generic wording and the def is destroyed without the collision being named (a `realpath` on both sides would close it; not done). It is deliberately **not** `blocked` — C79 asks whether the destination resolves to a regular file, and one full of record JSON does; separating that from the two legitimate exit-0 shapes needs a **content** predicate, which is a different rule. **THE RESULTING END STATE IS UNREPAIRABLE AND IS PRE-EXISTING C77, NOT NEW HERE:** the record now lives *inside* the def, so `readRecords` returns the def's payload hash, never-clobber skips it forever, `installed` names a file the same run destroys, and `doctor` reports both `✓ present` and `✓ no skew` over it. Tracked separately. The uninstall leftover claims only what the run can stand up — the record parsed from that path is the evidence the bytes were ever ours, so a link never written through is reported untouched rather than destroyed. *Provenance:* the ask, the rule and the uninstall shape are the maintainer's; the split disclosure and the record-leaf argument are Claude's; the plan-time defect, the false doc sentence and the unguarded destruction claim came from independent review. Covered by `test/init.test.ts`.

### Doctor's verdict (issue #151 — `src/cli.ts`, `test/doctor.test.ts`)

- **C75** (added 2026-08-03, issue #151; auth severity settled by maintainer decision the same day, after two independent reviews) — **`doctor` FAILS on the opencode BINARY and WARNS on opencode AUTH, and the split is drawn by what the check can actually see.** **The binary is a hard failure in two shapes.** A missing `opencode` is a `✗`/exit 1, not the `!` under `doctor: OK` it used to be: every `guild_*` tool spawns `opencode serve`, so there is nothing degraded about that state, and leaving it a warning inverted the ordering against the merely-unregistered MCP server, which has always failed. A binary that IS on PATH whose `--version` fails is the same verdict under a distinct message naming the **exit status**, because "not installed" and "installed and broken" have different remedies. **A spawn error is classified by ERRNO, not treated as "absent":** only `ENOENT` earns the not-found wording; `EACCES` and any other errno are reported as *could not execute opencode (`<code>`)*, since telling a user with a non-executable binary to install one sends them at the wrong problem. **AUTH PRESENCE is CHECKED and REPORTED, and it is a WARNING** (`opencodeAuthState`, `opencode auth list` — token-free, no model call), **skipped whenever the BINARY CHECK FAILED** — not-found and on-PATH-but-broken alike — so one cause never produces two lines. **The probe evidence is unchanged and still decides the parse.** The exit code of `auth list` **carries nothing — it is 0 in every state** (probed, opencode 1.18.11), so the printed counts are the only signal; **BOTH sources opencode reports are summed**, stored credentials and provider API-key environment variables (the `Environment` section is printed only when non-empty, so its absence is zero); and **anything unparseable, or a run that fails, is a `!` "could not determine"** — fail-open, since a doctor that cannot read the output has learned nothing. **WHY A WARNING AND NOT A FAILURE: the probe cannot see every way this state could be fine.** A provider declared directly in `opencode.json` is invisible to `auth list` in **at least two probed shapes** — an `apiKey` in the provider's options, and a local or OpenAI-compatible endpoint that needs **no credential at all** — and both print "0 credentials" with no `Environment` section while models answer normally. So a zero is evidence about what `auth list` can see, not proof that nothing can answer, and those configurations produce a false **alarm** rather than a false failure. **This is the posture this file already holds elsewhere, cited because consistency is the argument:** the MCP-registration check downgrades to a warning when the `claude` CLI is absent rather than call a working global setup broken, and **C72** keeps payload skew, drift and the unjudgeable state report-only. The binary checks stay hard because they have no such blind spot — no opencode, no serve, no call. **The parser residuals, stated rather than implied:** the counts are read as **line-final footers, last match winning**, which rejects an embedded `0 credentials-migrated` and an earlier stray count that an unanchored search would have taken; a **WHOLE-format** change leaves no footer at all and degrades to "could not determine", but a **PARTIAL** drift that keeps the credentials phrase while dropping or renaming the environment one reads as zero env vars, so the warning can fire spuriously. **Every failing line states a NEGATIVE:** `line(ok, msg)` selects the glyph and cannot rewrite the words, so a single positive sentence produced `✗ model policy present (…)` and `✗ 0/3 hardened agent defs present in …`; the agent-def line now mirrors the command-docs `— missing: <names>` form and the policy line reads `model policy MISSING — looked in: …`.
- **C76** (added 2026-08-03, issue #151) — **The MCP handshake's `serverInfo.version` is the RUNNING package's real version**, resolved by `packageVersion(PACKAGE_ROOT)` imported from `src/init.ts` — the same `import.meta.url`-derived root C72 defines as "shipped", so it is correct under `npx`, `npm i -g` and a source checkout alike and is not redirectable by a cwd or an env var. It was hardcoded `"0.0.0"`, so the one surface every MCP client sees identified nothing while README's bug-report checklist asked for it. **A resolution failure falls back to `"0.0.0"` rather than throwing** — `packageVersion` returns `""` on an unreadable `package.json` and never throws, and start-up must not break over a version string.

## G. Security doctrine invariants
*Oracle: `.opencode/agent/guild-*.md`; `AGENTS.md` Conventions/Gotchas; `check-agent-permissions.sh`; `verify-guild-*.sh`.*

> **Amended 2026-07-22 (permission realignment) — supersedes the original area-G freeze on the secret-glob model.** C48–C50 below are the amended text; the read paths (`guild-read`/`guild-research`) now carry a Claude review subagent's tools (`read`+`grep`+`glob`+web) with **no** secret-glob read-denies and **no** `grep`/`glob` denials, per the maintainer decision recorded in AGENTS.md Conventions (PARITY, the 2026-07-22 permission realignment). The default-deny-allowlist *shape* (C47) is unchanged; only the read-path allow-sets and the harness-difference justification changed.

- **C47** (amended 2026-07-23, issue #36 — `guild-watch` retired at M12) — The **three** hardened agent defs (`guild-read`/`guild-build`/`guild-research`; the fourth, `guild-watch`, retired with the witness — historical) are **default-deny allowlists**: `"*": deny` floor (no `"*": allow`), `mode: all`, re-allowing exactly their intended tools. Preserved **unchanged, opencode-side** by the rewrite.
- **C48** (amended 2026-07-22; `guild-watch` row marked historical 2026-07-23, issue #36; **`guild-build`'s read amended 2026-07-29, issue #29**) — Per-agent allow-sets: `guild-read` = read + grep + glob + webfetch/websearch (**no secret-glob read-denies**); `guild-research` = **identical** to `guild-read`; `guild-build` = edit/write/patch/bash **+ a plain `read: allow`, with no secret-glob read-denies either**. (`guild-watch` = read `modelguild/logs/**` only, inverted map, all else incl. secrets denied — **retired at M12** with the witness; historical.) **NO shipped agent def carries a secret-path read-deny** — on the read paths that is the 2026-07-22 realignment; on `guild-build` it is the 2026-07-29 maintainer decision closing issue #29's first residual. Direction, recorded so no later reading inverts it: that decision **removed a layer**. The carve-outs were defense-in-depth on the read TOOL and `bash` — allowed on this agent by design — bypassed them with `cat`, so they implied a credential boundary the write path has never had; removing them costs the *tool-native* refusal a compliant model used to get, and buys a map that states what is true. `check-agent-permissions.sh` asserts the plain-`read: allow`-no-submap shape on all three, and `verify-guild-build.sh` asserts no former secret glob resolves to deny.
- **C49** (amended 2026-07-29, issue #29) — Honest-guarantee statements are part of the contract: `guild-read`/`guild-research` are **not** exfiltration or credential boundaries — they can read any repo file **including credentials** and reach the web, so a secret can egress to a third-party model provider; the no-write/no-`task` scoping is the read-only ROLE, not a confidentiality floor. **`guild-build` is not a credential boundary either, and no longer claims to be**: its remaining non-mutation denies are **defense-in-depth, not by construction** (`bash` routes around them), and the "secret reads are denied" claim was **withdrawn** with the carve-outs (C48) rather than kept as a comforting half-truth — it can read any repo file, credentials included. The human diff review is the boundary on this path, as it was before. (`guild-watch`'s `bash`/`grep`/`glob`/egress denials were load-bearing — **retired at M12** with the witness, issue #36; historical.)
- **C50** (amended 2026-07-22) — `grep`/`glob` are **allowed** on the read paths (review-subagent parity); the former "opencode grep bypasses `read:` secret globs" harness difference was **retired as circular** (it only bites if you fence secrets, which the read-only role does not). The **current** named harness difference is that a read on the external read paths **egresses to a third-party vendor** (a Claude subagent's read stays inside Anthropic) — surfaced by a security scan and accepted as an informed trusted-repo tradeoff. MCP tools are covered by the `"*": deny` floor.
- **C51** — **PARITY rule:** asymmetry is default-deny; do not restrict a non-Claude path more than the Claude path for the same task without a named harness difference. Vendor is not a threat model. `--auto` is a proven no-op for hardened agents and must not justify write-path friction.
- **C52** — External model output (including a delegate's report **and its diff**) is untrusted data for Claude to reason over or verify — never a command to execute.

## H. Exit-code table (`ask.sh`)
*Oracle: `modelguild/ask.sh`; `AGENTS.md` header (note discrepancy in Report).*

- **C53** — `0` success; opencode's own non-zero status is propagated verbatim.
- **C54** — `1` usage error (missing prompt, missing flag value).
- **C55** — `2` model id starts with `-` (from env/config).
- **C56** — `3` model denied by policy; `4` model gated `ask` unconfirmed.
- **C57** (amended 2026-07-23, issue #36 — `--watch`/exit-8 retired; exit-5 is now the C16 def-missing refusal) — `5` required-hardened def missing — in the TS/MCP layer this is the **unconditional C16 refusal** (`guild_consult`/`guild_panel`/`guild_research`/`guild_delegate` when the hardened def is absent from **both** agent dirs; structured `agent-def-missing`, exit-analogue 5, no model call, nothing logged). The bash-era `--watch` uses of exit 5 (always-5 for `--watch`, incl. bad watch log-dir scope) and the exit `8` uses (`--watch` model unset, or `--watch` on a Claude model without `GUILD_CONFIRMED=1`) **retired with the witness at M12**; historical.
- **C58** — `124` `$GUILD_TIMEOUT` hit (via `timeout`/`gtimeout`); `127` opencode not found, or `jq` missing on the `--emit-session` path. (`log.sh`: `0` ok, `2` usage, `7` integrity failure.)

---

## How this spec is held (historical: "how parity was declared")

During the rewrite, each TypeScript milestone PR **cited the C-numbers it implemented** and stated, for
each, that the ported behavior matched the bash oracle (test, live probe, or cross-verified log), with
any **intentional deviation** recorded with a reason. That parity process is complete. The bash
cross-verification the TS suite used ("until bash retires", CONTRACT M3) was removed at M12 when the
bash layer was deleted; the TS verifier now stands alone as the reference.

**Going forward**, a change to any behavior an item names must keep this document and the TS suite in
agreement — the code wins, and the document is fixed to match. Recorded deviations from the original
bash behavior (transport artifacts and superseded items) are noted inline above.
